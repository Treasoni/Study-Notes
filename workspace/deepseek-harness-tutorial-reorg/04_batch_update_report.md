# 批量更新报告：DeepSeek-Harness 教程排版重排

## 汇总

| 指标 | 数量 |
|---|---|
| 处理文件 | 9 |
| 更新文件 | 9 |
| 跳过文件 | 0（`example-plugin/` 脚手架为代码，不在 Markdown 范围） |
| 需复核 | 0 |
| 输出模式 | patch-in-place |

## 变更清单

**7 篇正文**：frontmatter `title` 与 H1 统一为 `DeepSeek-Harness 插件开发 · 第 X 章：主题——副题`（主章）或 `DeepSeek-Harness 配置专册 · 主题——副题`（配套专册），两者一致。

**README.md**：
- 「这是什么」「产出形态」计数 → 5 主章 + 2 配置专册；
- 分册清单补录 04·配套《配置实战》；
- 推荐阅读顺序（主路径 + 急用路径）补《配置实战》；
- 系列约定：「节编号与章号同步」→「主章同步；配套专册独立编号（1–N）」。

**DeepSeek-Harness MOC.md**：
- 顶部自述 → 5 主章 + 2 配置专册 + 系列导览；
- 04 实战项目两篇标注 04·A（写插件）/ 04·B（配置接入）；
- 学习路径图补「03/04 各含 1 篇配置专册」。

## 风险与结论

- 所有改动为标题与导航；正文技术内容零改动；
- 双链按文件名解析，标题变化不破坏链接；
- 主章 01–05 编号未变，正文内「第 X 章」「5.1」引用保持有效；配套专册内部编号不变。

## 遗留 / 建议
- 本目录排版重排完成。用户另指出 `AI学习/Claude Code 教程/` 可能同样存在导航/标题不一致问题（见下一条独立任务处理）。
