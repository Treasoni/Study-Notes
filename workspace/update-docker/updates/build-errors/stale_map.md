# stale_map — docker容器搭建错误的知识讲解

> 更新范围：仅补 frontmatter（用户确认）
> 日期：2026-08-04

## 保留

| 区块 | 说明 |
|------|------|
| 正文全部 | docker compose 配置修改行为说明 2026 年仍适用（compose v2 行为一致） |
| 双链 | [[Docker MOC]]、[[docker容器如何更新]]、[[docker里的GID和UID]]、[[docker进行代理]] 有效 |
| 原有 tags | `docker` |

## 需要更新

| 区块 | 说明 |
|------|------|
| frontmatter | 缺 `title`、`created`、`updated`、`status`、`source_project`，补齐 |

## 需要删除

- 无

## 需要新增

| 区块 | 说明 |
|------|------|
| frontmatter `title` | `docker容器搭建错误的知识讲解` |
| frontmatter `created` | `2026-07-28`（vault 文件时间戳） |
| frontmatter `updated` | `2026-08-04` |
| frontmatter `status` | `updated` |
| frontmatter `source_project` | `study-notes` |
| `## 更新记录` | 记录本次变更 |
