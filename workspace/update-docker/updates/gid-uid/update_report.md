# update_report — docker里的GID和UID

> 日期：2026-08-04
> 状态：✅ 已完成（仅补 frontmatter）

## 变更摘要

- 补齐 YAML frontmatter：`title`、`created`（2026-07-28）、`updated`（2026-08-04）、`status`（updated）、`source_project`（study-notes）。
- 追加 `## 更新记录`。
- 正文未改动。

## 来源

- 无外部资料收集（纯 frontmatter 补齐）。

## 未处理风险

| 风险 | 说明 |
|------|------|
| 正文疑似损坏行 | 「先纠正一个小点」处 `> **Docker 里常见的是 UID / GID>` 缺结束 `**`，Obsidian 渲染可能异常，属原有问题，建议后续修复 |
| created 日期为近似值 | 原文无 created，采用 vault 文件时间戳 2026-07-28，非原始创作日期 |
