# stale_map — docker里的GID和UID

> 更新范围：仅补 frontmatter（用户确认）
> 日期：2026-08-04

## 保留

| 区块 | 说明 |
|------|------|
| 正文全部 | UID/GID 概念与示例 2026 年仍适用，无时效性问题 |
| 双链 | [[Docker MOC]]、[[docker容器搭建错误的知识讲解]]、[[docker容器如何更新]] 有效 |
| 原有 tags | `docker` |

## 需要更新

| 区块 | 说明 |
|------|------|
| frontmatter | 缺 `title`、`created`、`updated`、`status`、`source_project`，补齐 |

## 需要删除

- 无

## 需要新增

| 区块 | 说明 |
|------|------|
| frontmatter `title` | `Docker 里的 UID / GID` |
| frontmatter `created` | `2026-07-28`（vault 文件时间戳） |
| frontmatter `updated` | `2026-08-04` |
| frontmatter `status` | `updated` |
| frontmatter `source_project` | `study-notes` |
| `## 更新记录` | 记录本次变更 |

## 标记（未处理）

- 正文「先纠正一个小点」处 `> **Docker 里常见的是 UID / GID>` 疑似损坏（缺结束 `**`），属原有问题，本次按「仅 frontmatter」范围不处理，建议后续修复。
