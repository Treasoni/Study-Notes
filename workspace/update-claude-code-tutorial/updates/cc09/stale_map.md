# 过时映射（Stale Map）— cc09 Claude Code Memory 完整指南

> 更新目标：同步到 2026-08 现状。来源：SB-23（CLAUDE.md 维护建议）+ 官方 Memory 文档（code.claude.com/docs/en/memory）2026-08 核对。

## 保留（Keep）

| 位置 | 理由 |
|------|------|
| 概述（一句话定义 / 通俗比喻） | 现行，未过时 |
| 核心概念「是什么 / 为什么需要」 | 现行，未过时 |
| 通俗理解「🎯 比喻」主体 | 现行，仅在其后新增 `[!tip] 大白话` |
| Memory 命令快速参考主体（`/init`、`#` 前缀、`/memory` vs `/init` 对比） | 现行 |
| `CLAUDE_CODE_NEW_INIT` 增强交互 | 现行（官方为 `=1`，`=true` 兼容） |
| Memory 架构层级结构（mermaid + 完整优先级表） | 现行，2026-08 仍准确 |
| 模块化规则系统（目录结构 / paths / 子目录 / 符号链接） | 现行 |
| Auto Memory「是什么」、存储位置、主题文件按需加载 | 现行 |
| 导入外部内容（`@` 语法 / 审批对话框 / 代码块不解析） | 主体现行，仅深度改 4 层 |
| `claudeMdExcludes` | 现行 |
| 实用示例 1–3 | 现行 |
| 最佳实践主体（✅/❌ 表格其余行、选择记忆级别） | 现行 |
| 安装配置（`/init` 与手动创建） | 现行 |
| 与其他概念的关系、常见问题其余条目 | 现行 |
| 个人笔记、相关文档、参考资料 | 保留 |
| 原结构与写作风格、Callout 用法 | 保留 |

## 更新（Update）

| 位置 | 现状 | 改为 |
|------|------|------|
| frontmatter `updated` | 2026-07-12 | 2026-08-10（`status: updated` 保持） |
| Auto Memory「加载行为」 | 启动时加载 `MEMORY.md` 前 200 行 | 前 200 行 **或前 25KB**（取先到者）；`MEMORY.md` 是记忆目录索引 |
| `autoMemoryDirectory` 作用域 | 只能在用户级或本地设置配置，不能在项目或托管策略配置 | 可从任意 settings 层级读取（用户/项目/本地/策略/`--settings`）；项目级需接受工作区信任对话框；值须绝对路径或以 `~/` 开头 |
| 控制 Auto Memory | env `0`=强制开启 / `1`=强制关闭 / 未设置=启用 | 三种官方方式：`/memory` 会话内开关、`autoMemoryEnabled` 设置、`CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` |
| 导入深度（导入特性 + FAQ） | 最大 5 层递归导入 | 最大 4 层递归导入 |
| 最佳实践「❌ 不应该做：太长」 | 500 行以内 | 单个 CLAUDE.md 目标 200 行以内 |
| `/memory` 命令功能 | 仅「在编辑器中编辑记忆文件」 | 列出用户/项目记忆文件位置、开关 Auto Memory、打开 Auto Memory 文件夹；GUI 编辑器 v2.1.216+ 行为 |
| FAQ「如何调试 Memory 加载问题」 | 步骤 1 用 `/status` | 改用 `/context` 核对 Memory files，补充 `/memory` |
| 原文件乱码 3 处（第 26/42/491 行） | `编���规范` / `编���规范文档` / `��终记录错误` | `编码规范` / `编码规范文档` / `始终记录错误` |

## 删除（Delete）

| 位置 | 内容 | 原因 |
|------|------|------|
| 控制 Auto Memory | `CLAUDE_CODE_DISABLE_AUTO_MEMORY=0 强制开启` 语义及示例 | 未见于官方文档；官方只明确 `=1` 关闭；社区报告 `=0` 仅停用自动记忆写入而非完整功能。改为 `[!warning]` 说明。 |

## 新增（Add）

| 位置 | 新增内容 | 来源 |
|------|---------|------|
| 核心概念·通俗理解 | `[!tip] 大白话`：三层记忆体系比喻（CLAUDE.md 说明书 / Auto Memory 工作笔记本 / 参考文档资料柜） | 用户偏好 |
| Memory 命令快速参考 | `/context`（核对已加载记忆文件）、`/doctor`（CLAUDE.md 健康体检 v2.1.206+）两行 | 官方文档 |
| Auto Memory「版本要求」 | `modified` 时间戳字段（ISO 8601，需 v2.1.214+） | 官方文档 |
| 最佳实践·❌ 表后 | `[!tip]` `/doctor` 健康体检：裁剪已提交 CLAUDE.md（删可推导内容）、合并重复记忆文件、标记慢 hooks | SB-23, SB-10 |
| 常见问题 | `Q: CLAUDE.md 太长或重复怎么办？`（200 行 / `/doctor` 裁剪 / `paths:` 拆分） | 官方文档 |
| 文末 | 「更新记录」2026-08-10 条目 | 流程要求 |
