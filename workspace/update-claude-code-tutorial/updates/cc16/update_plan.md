# update_plan — cc16「CLAUDE.md 使用指南」

> 生成：2026-08-10
> 更新目标版本：2026-08 现状（v2.1.193 ~ v2.1.226）
> 策略：8 处局部 patch；**不修改原 vault 文件**；产物写入 `updates/cc16/`。

## 执行清单

| 步骤 | 位置 | 操作 | 产物 |
|------|------|------|------|
| P1 | `updated_note.md` | 按下列 patch 生成完整更新稿 | `updated_note.md` |
| P2 | — | 核对 frontmatter YAML 特殊字符（`title: CLAUDE.md 使用指南` 含 `.` 与空格，无冒号/井号，无需引号） | — |

## Patch 明细

### Patch A — frontmatter
- `updated: 2026-07-27` → `updated: 2026-08-10`
- `status: updated` 维持不变（已是更新态，语义正确）

### Patch B — 核心概念新增 `[!tip] 大白话`
- 位置：「什么是 CLAUDE.md」的「为什么需要」列表之后。
- 内容：把 CLAUDE.md 比作给 Claude Code 的「上岗说明书」。

### Patch C — 最佳实践 §1「保持简洁（200 行以内）」→「保持精简（200 行以内 / 25KB 内）」
- 标题与正文改为 200 行以内**或 25KB 以内**。
- 加 `[!tip] 大白话`：不是越厚越好，要点速查卡才有效。

### Patch D — 最佳实践 §2「项目特异性」补充「可推导内容不写」
- 补充：不写代码库/工具本身能推导的内容（函数签名、框架文档、命令帮助）。
- 提及 `/doctor` 会删这类冗余。
- 加 `[!tip] 大白话`：能自己查到的东西别写。

### Patch E — 诊断命令表 `/doctor` 行 + 追加说明 callout
- 行改为：`/doctor`（=`/checkup`）全量环境体检：诊断安装健康、未用 skills/MCP/插件、CLAUDE.md 裁剪建议、慢 hooks 标记。
- 表后追加 `[!note]`：/doctor 会提议裁剪已提交的 CLAUDE.md（删可推导内容、去重、合并重复记忆文件、标记慢 hooks）。

### Patch F — 规则加载优先级后新增 `[!warning]` 工作区信任说明
- 内容：项目级 `./CLAUDE.md` 只在项目目录被标记为可信工作区后才会被读取；未信任时不加载项目级 CLAUDE.md 及其规则。

### Patch G — 模板与示例·完整示例前新增 `[!note]`
- 内容：完整示例仅作结构参考，实际仍应控制在 200 行 / 25KB 内。

### Patch H — 更新记录追加 `2026-08-10` 条目
- 置顶追加本次变更摘要，保留 `2026-07-27` 旧条目。

## 验证要点

- [x] 原文件结构（章节顺序、表格、代码块）保持不变，仅局部替换/插入。
- [x] 列表内未嵌套表格（所有新增均为 callout，无新表格插入列表）。
- [x] frontmatter YAML 无需加引号（无 `:`/`#` 等特殊字符）。
- [x] 仅应用 SB-23（cc16 适用条目），未越界引入其他 SB 条目。
- [ ] 原 vault 文件未被修改（仅写 `updates/cc16/` 产物）。
