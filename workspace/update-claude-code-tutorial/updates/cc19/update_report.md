# cc19 更新报告（update_report）

> 笔记：LLM Prompt Caching 提示缓存 · note_id：cc19
> 更新日期：2026-08-10
> 原状态：draft → 本次更新为 updated

## 更新摘要

对高级功能篇《LLM Prompt Caching 提示缓存》做 2026-08 现状同步，并按 update_goal 将 draft 补全为结构完整笔记。共定位 **13 处过时/待更新点**（见 stale_map S1–S13），全部按共享来源库局部 patch，**未删除任何段落**。主要变更：

1. **模型/上下文（核心）**：新增「1M 上下文与更大的缓存窗口」小节——Claude Sonnet 5 / Opus 5 原生 1M token 上下文（促销价 $2/$10 每 Mtok 至 2026-08-31）；补充 `CLAUDE_CODE_DISABLE_1M_CONTEXT=1` 强制回 200K 自动压缩，并注明该开关不影响缓存本身（SB-01 / SB-20）。
2. **缓存命中优化**：新增「长上下文分段缓存（1M 时代）」实践（第 5 条，原「监控缓存效果」顺延为 6）；破坏缓存表新增「切换 `CLAUDE_CODE_DISABLE_1M_CONTEXT`」「触发自动压缩」两行；诊断表新增「自动压缩触发」行；✅ 保持缓存表新增「1M 窗口内」行（SB-20）。
3. **本项目 prompt-cache 规则**：在「项目中的缓存优化实践」中明确稳定前缀在前、动态内容在后与 5 分钟 TTL 缓存断点的对应关系。
4. **结构补全（draft → 完整）**：补「小结」「常见问题（FAQ）」；新增误区「1M 上下文就不需要缓存了」；参考资料补充官方 changelog 链接。
5. **修正**：「三层缓存结构」标题与实际五层不符 → 改为「五层缓存结构」。
6. **写作规范**：核心概念（命中/未命中、缓存断点、1M 窗口、自动压缩）补 `[!tip] 大白话`；列表内无嵌套表格；YAML 无特殊字符待引号处理。
7. **frontmatter**：`updated` → 2026-08-10，`status` → `updated`；追加 2026-08-10 更新记录。

## 引用来源

| 条目 | 来源 | 用途 |
|------|------|------|
| SB-01 | code.claude.com/docs/en/changelog | Sonnet 5 默认模型、原生 1M 上下文、促销价 $2/$10 至 2026-08-31 |
| SB-20 | code.claude.com/docs/en/changelog | `CLAUDE_CODE_DISABLE_1M_CONTEXT` 强制 200K 自动压缩、长上下文分段缓存策略 |

## 未处理风险 / 需人工复核项

1. **促销定价时效**：Sonnet 5 促销价 $2/$10 每 Mtok 截止 **2026-08-31**，届时需复核定价描述。
2. **模型默认值跨篇一致性**：本笔记引用 Sonnet 5 / Opus 5 为默认模型，若同批其他 cc 笔记（cc05/cc12 等）同步过，请统一核对。
3. **原笔记既有双链未逐篇核对**：相关文档区引用 [[settings.json 配置详解]]、[[如何编写Skills]] 等，未逐一验证其内容是否与本篇新事实冲突，建议在批量更新时按其 note_id 处理。
4. **「缓存机制表」沿用原文 1024 tokens / 5 分钟 TTL**：共享资料库未给出新的缓存参数，按现行值保留；若官方后续调整 TTL/最小块，需再更新。
5. **MOC 未更新**：moc_path 为 none，P5 统一处理。

## 结论

过时点已全部按共享来源库处理，未引入未覆盖的新事实；draft 已补全为结构完整笔记（含小结/FAQ/更新记录）。

**是否需要 needs-review：是** —— 涉及模型默认值（Sonnet 5 / Opus 5 原生 1M）与促销定价（至 2026-08-31）等时效信息，且原笔记为 draft 首次补全为正式态，建议用户审阅 `updated_note.md` 后写回原 vault 文件。
