# cdx04 结构对照表：04 Skills 技能系统 → Skills 技能系统

> 源文件：`AI学习/Codex/04 Skills 技能系统.md`
> 目标文件：`AI学习/Codex/03-进阶应用/Skills 技能系统.md`

## 旧结构 → 新结构

| 旧结构（书章节风格） | 新结构（Claude Code 教程风格） | 处理方式 |
|------|------|------|
| `# 第四章：Skills 技能系统 —— 创建、注册与共享` | `# Skills 技能系统` | 重命名标题 |
| （无） | `> [!info] 文档定位` | 新增（紧随 H1 之后） |
| 前言段落（"第三章我们深入了…" / "本章将全面解析…"） | `## Skills 是什么` 首段 | 移除章回衔接叙述，保留技术含义 |
| `> [!note] 关键发现：Skills 可跨工具共享` | `> [!note] 关键发现：Skills 可跨工具共享` | Callout 保留，位置调整至 Skills 是什么 |
| `### 1. Skills 是什么？` | `## Skills 是什么` | 重编号为 `##` |
| （表格：AGENTS.md vs Skill） | `### Skills 与 AGENTS.md 的区别` 下表格 | 表格保留，归入子节 |
| `### 2. Skills 目录结构` | `## Skills 目录结构` | 重编号为 `##` |
| `### 3. SKILL.md 深度解析` | `### SKILL.md 深度解析` | 重编号为 `###`（并入「Skills 目录结构」） |
| `> **最佳实践**：…`（内嵌引用块） | `> [!tip] 最佳实践` | 格式化为 Callout，内容保留 |
| `### 4. 发现路径：五层作用域` | `## 发现与加载机制` → `### 发现路径：五层作用域` | 重编号并归入主题 |
| `### 5. 渐进式延迟加载机制` | `### 渐进式延迟加载机制` | 重编号为 `###`（并入「发现与加载机制」） |
| `### 6. 启用与禁用 Skill` | `## Skill 配置与管理` → `### 启用与禁用 Skill` | 重编号并归入主题 |
| `> **Claude Code 对照**：…`（内嵌引用块） | `> [!note] Claude Code 对照` | 格式化为 Callout，内容保留 |
| `### 7. agents/openai.yaml：Codex 特有的扩展层` | `### agents/openai.yaml：Codex 特有的扩展层` | 重编号为 `###`（并入「Skill 配置与管理」） |
| `### 8. 内置创建工具：skill-creator 与 skill-installer` | `### 内置创建工具：skill-creator 与 skill-installer` | 重编号为 `###`（并入「Skill 配置与管理」） |
| `### 9. Skill 共享方案` | `## Skill 共享方案` | 重编号为 `##` |
| （共享方案内嵌的两个代码块 + 兼容性表格） | `### 方案一：符号链接共享（推荐）` / `### 方案二：独立技能仓库（团队级）` / `### 兼容性对照` | 拆分为子节，内容保留 |
| `> **本章小结**：…`（内嵌引用块） | `## 小结` | 从内嵌块提升为独立章节，正文保留 |
| （无） | `## 常见问题`（3 个 Q&A） | 新增（内容均源自原文：跨工具共享、五层发现作用域、禁用机制） |
| （无） | `## 最佳实践`（Do's / Don'ts） | 新增（从原文「最佳实践」「Claude Code 对照」「兼容性对照」提炼） |
| （无） | `## 相关文档`（表格） | 新增（替换旧导航块） |
| （无） | `## 参考资料` | 新增（官方 Codex 链接） |
| （无） | `## 更新记录` | 新增 |

## 新增 / 删除 / 重命名清单

### 新增（结构性、非技术内容）
- `> [!info] 文档定位` Callout
- `## 常见问题`（3 个 Q&A）
- `## 最佳实践`（Do's / Don'ts）
- `## 相关文档` 表格
- `## 参考资料`
- `## 更新记录`
- 主题分组标题：`## 发现与加载机制`、`## Skill 配置与管理`；`### Skills 与 AGENTS.md 的区别`、`### 方案一 / 方案二 / 兼容性对照` 等子节标题
- 前言的书籍衔接句已并入 `## Skills 是什么` 首段，仅保留技术含义，去掉章回导航叙述

### 删除
- `> [!note] 导航` 块（`[[03 AGENTS.md 分层体系|← 上一章]] | [[05 Agents 与 MCP|下一章 →]]`）
- 旧章号前缀（`第四章：`、`### N.`、`#### N.M`）
- 章回衔接句（"第三章我们深入了…"、"本章将全面解析…"）

### 重命名 / 移动
- 标题：`第四章：Skills 技能系统 —— 创建、注册与共享` → `Skills 技能系统`
- 位置：`AI学习/Codex/04 Skills 技能系统.md` → `AI学习/Codex/03-进阶应用/Skills 技能系统.md`
- frontmatter：`title` → `Skills 技能系统`；`tags` → `[codex, ai, 工具使用, 进阶应用, skills]`
- 内嵌引用块 → Callout（内容未变）：`> **最佳实践**` → `> [!tip] 最佳实践`；`> **Claude Code 对照**` → `> [!note] Claude Code 对照`
- `> **本章小结**` 内嵌块 → `## 小结`

## 保留的实质性内容（未删改）
- 全部代码块：目录结构树、SKILL.md frontmatter、Claude Code 扩展字段、五层作用域、REPO 向上遍历树、`config.toml` 禁用配置、`agents/openai.yaml`、skill-creator / skill-installer 命令、符号链接共享、独立技能仓库脚本
- 全部表格：AGENTS.md vs Skill、五层作用域路径、渐进式加载对比、兼容性对照
- 全部 Callout / 引用块正文
- frontmatter 中 `source_project: codex-config`、`created: 2026-07-31` 不变；`updated` 更新为 2026-08-10，`status` 更新为 updated
