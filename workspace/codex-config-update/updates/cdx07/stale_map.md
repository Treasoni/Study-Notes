# cdx07 结构映射表（旧结构 → 新结构）

> 笔记：`AI学习/Codex/07 CLI 与调试.md` → `AI学习/Codex/02-基础功能/Codex CLI 与调试.md`
> 任务：重排套用 Claude Code 教程模板，内容保留。

## 1. Frontmatter 变更

| 字段 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `title` | `Codex 完整配置体系` | `Codex CLI 与调试` | 去章名序号，改为独立标题 |
| `tags` | `[codex, claude-code, configuration]` | `[codex, ai, 工具使用, 基础功能, cli]` | 套用模板标签体系 |
| `updated` | `2026-07-31` | `2026-08-10` | 更新日期 |
| `status` | `completed` | `updated` | 状态改为已更新 |
| `created` | `2026-07-31` | `2026-07-31` | 保留 |
| `source_project` | `codex-config` | `codex-config` | 保留 |

## 2. 正文标题映射

| 旧结构 | 新结构 | 变更类型 |
|--------|--------|----------|
| `# 第七章：CLI 与调试 —— 日常操作与故障排查` | `# Codex CLI 与调试` | 重命名 |
| （无） | `> [!info] 文档定位` | 新增（紧跟 H1） |
| 开头说明段落（"前六章我们把 Codex 的配置体系拆了个遍……"） | 合并进「文档定位」callout | 改写（去掉章序引用，语义保留） |
| `### 1. 核心 CLI 命令`（含两段代码块 + 经验法则） | `## 核心 CLI 命令` + `### 交互式 REPL 与单次执行` + `### 工作目录与模型选择` + `### 状态查看、配置覆盖与配置档` + `### 选择原则` | 降级+去序号+拆分 |
| `### 2. 交互式命令` | `## 交互式命令` | 降级+去序号 |
| `### 3. 环境变量` | `## 环境变量` + `### .env 加载规则` | 降级+去序号+拆出子节 |
| `### 4. 调试与验证方法` | `## 调试与验证方法` | 降级+去序号 |
| `### 5. 配置审计技巧` | `## 配置审计技巧` + `### 快速诊断清单` + `### 常见故障案例` | 降级+去序号+拆出子节 |
| `> **本章小结**：……` | `## 小结`（正文段落） | 改写为小结章节 |
| `> [!note] 导航` | `## 相关文档`（wikilink 表格） | 替换（移除旧导航） |

## 3. 新增区块

| 区块 | 说明 |
|------|------|
| `## 常见问题` | 新增 3 条 Q&A（配置不生效、如何确认加载内容、CODEX_HOME 作用），内容均从正文提炼 |
| `## 最佳实践`（`### Do's` / `### Don'ts`） | 新增，Do's 4 条、Don'ts 4 条 |
| `## 小结` | 新增，正文"本章小结"精炼 |
| `## 相关文档` | 新增，wikilink 表格（替代旧导航） |
| `## 参考资料` | 新增，仅官方 Codex 文档链接 |
| `## 更新记录` | 新增 |

## 4. 移除 / 替换

| 旧内容 | 处理 | 说明 |
|--------|------|------|
| `> [!note] 导航 > [[06 Hooks 与插件\|← 上一章]] \| [[08 对照表与迁移实战\|下一章 →]]` | 移除 | 旧的书籍式章节目录导航块，替换为「相关文档」表格 |
| `### N.` 序号标题 | 移除序号 | 转为 `##` / `###` 标题 |
| 章号前缀（第七章） | 移除 | 改为独立主题标题 |

## 5. 保留内容（未改动）

- 正文全部代码块：核心 CLI 命令两段、交互式命令、环境变量、调试验证方法、快速诊断清单
- 全部段落：`--cd` 重要性说明、`.env` 加载规则 bullet 列表、经验法则 bullet 列表
- 全部表格：常见故障案例表（4 行 4 列）
- 全部 callout：本章小结（改写为小结章节，语义保留）

## 6. 格式规范

- 大主题用 `---` 分隔
- 仅使用 `##` 与 `###` 两级标题
- 文档定位 callout 紧跟 H1 之后
- 全部内容为中文
