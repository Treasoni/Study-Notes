# cdx03 结构对照表：03 AGENTS.md 分层体系 → AGENTS.md 分层体系

> 源文件：`AI学习/Codex/03 AGENTS.md 分层体系.md`
> 目标文件：`AI学习/Codex/03-进阶应用/AGENTS.md 分层体系.md`

## 旧结构 → 新结构

| 旧结构（书章节风格） | 新结构（Claude Code 教程风格） | 处理方式 |
|------|------|------|
| `# 第三章：指令与规则 —— AGENTS.md 分层体系` | `# AGENTS.md 分层体系` | 重命名标题 |
| （无） | `> [!info] 文档定位` | 新增（紧随 H1 之后） |
| `### 1. AGENTS.md 是什么？` | `## AGENTS.md 是什么` | 重编号为 `##` |
| （原文内嵌引用块） | `> [!note] Claude Code 对照` | 格式化为 Callout，内容保留 |
| `### 2. 发现机制与分层级联` | `## 发现机制与分层级联` | 重编号为 `##` |
| `#### 2.1 发现路径` | `### 发现路径` | 重编号为 `###` |
| `#### 2.2 合并规则：从根到叶拼接` | `### 合并规则：从根到叶拼接` | 重编号为 `###` |
| `#### 2.3 文件优先级` | `### 文件优先级` | 重编号为 `###` |
| `### 3. 与 CLAUDE.md 的兼容：fallback 机制` | `## 与 CLAUDE.md 的兼容：fallback 机制` | 重编号为 `##` |
| `### 4. 容量限制与最佳实践` | `## 容量限制与最佳实践` | 重编号为 `##` |
| `### 5. 特殊段落` | `## 特殊段落` | 重编号为 `##`；子块拆为 `### Code Review Rules` / `### Working Agreements` |
| （原文内嵌引用块） | `> [!note] 区分建议` | 格式化为 Callout，内容保留 |
| `### 6. Starlark 规则系统` | `## Starlark 规则系统` | 重编号为 `##` |
| （原文内嵌引用块） | `> [!note] 核心差异一句话总结` | 格式化为 Callout，内容保留 |
| `### 7. 验证工具` | `## 验证工具` | 重编号为 `##` |
| （无） | `## 常见问题`（3 个 Q&A） | 新增（内容均源自原文：AGENTS.md vs CLAUDE.md、32 KiB 容量、`codex status` 审计） |
| （无） | `## 最佳实践`（Do's / Don'ts） | 新增（从原文「容量限制与最佳实践」「Starlark 确定性」「特殊段落区分」提炼） |
| `> **本章小结**：…`（内嵌引用块） | `## 小结` | 从内嵌块提升为独立章节，正文保留 |
| （无） | `## 相关文档`（表格） | 新增（替换旧导航块） |
| （无） | `## 参考资料` | 新增（官方 Codex 链接） |
| （无） | `## 更新记录` | 新增 |

## 新增 / 删除 / 重命名清单

### 新增（结构性、非技术内容）
- `> [!info] 文档定位` Callout
- `## 常见问题`（3 个 Q&A）
- `## 最佳实践`（Do's / Don'ts）
- `## 相关文档` 表格
- `## 参考资料`
- `## 更新记录`
- 前言的书籍衔接句（"前两章我们分别建立了…"）已并入 `> [!info] 文档定位`，仅保留技术含义，去掉章回导航叙述

### 删除
- `> [!note] 导航` 块（`[[02 config.toml 核心配置|← 上一章]] | [[04 Skills 技能系统|下一章 →]]`）
- 旧章号前缀（`第三章：`、`### N.`、`#### N.M`）

### 重命名 / 移动
- 标题：`第三章：指令与规则 —— AGENTS.md 分层体系` → `AGENTS.md 分层体系`
- 位置：`AI学习/Codex/03 AGENTS.md 分层体系.md` → `AI学习/Codex/03-进阶应用/AGENTS.md 分层体系.md`
- 内嵌引用块 → Callout（内容未变）：`> **Claude Code 对照**` → `> [!note] Claude Code 对照`；`> **区分建议**` → `> [!note] 区分建议`；`> **核心差异一句话总结**` → `> [!note] 核心差异一句话总结`
- `> **本章小结**` 内嵌块 → `## 小结`

## 保留的实质性内容（未删改）
- 全部代码块：发现路径、根到叶拼接图、fallback 查找图、容量分层树、`.codex/rules/` 目录树、Code Review Rules 示例、Working Agreements 示例、Starlark `safety.rules` 完整示例、`codex status` / `codex --cd` 验证命令
- 全部表格：文件优先级表、操作类型表（allow/prompt/forbidden）、Codex vs Claude Code 规则对比表
- 全部 Callout / 引用块正文
- frontmatter 中 `source_project: codex-config`、`created: 2026-07-31` 不变；`updated` 更新为 2026-08-10，`status` 更新为 updated
