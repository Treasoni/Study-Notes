# cdx08 结构映射：对照表与迁移实战

## 结构映射表（旧结构 → 新结构）

| 旧结构（来源） | 新结构（目标） | 变更类型 |
|------|------|------|
| `title: "Codex 完整配置体系"` | `title: 对照表与迁移实战` | renamed |
| `tags: [codex, claude-code, configuration]` | `tags: [codex, ai, 工具使用, 高级功能, 迁移]` | renamed |
| `updated: 2026-07-31` / `status: completed` | `updated: 2026-08-10` / `status: updated` | renamed |
| `# 第八章：完整对照表与从 Claude Code 迁移实战` | `# 对照表与迁移实战` | renamed |
| （无） | `> [!info] 文档定位`（紧随 H1） | added |
| 章首两段引言（悬而未决的问题 + 本章只做一件事） | `## 完整对照表` 下的引言段 | moved（内容原样保留） |
| `### 1. 完整对照表` | `## 完整对照表` | renamed（层级 3→2，编号去除） |
| `#### 文件与路径对照` | `### 文件与路径对照` | renamed（编号去除） |
| `#### 指令与规则对照` | `### 指令与规则对照` | renamed（编号去除） |
| `#### 技能系统对照` | `### 技能系统对照` | renamed（编号去除） |
| `#### 扩展与安全对照` | `### 扩展与安全对照` | renamed（编号去除） |
| `#### Codex 独有功能` | `### Codex 独有功能` | renamed（编号去除） |
| `> **核心结论**` | `> **核心结论**`（紧随对照表） | kept |
| `### 2. 迁移四步走策略` | `## 迁移四步走策略` | renamed（层级 3→2，编号去除） |
| `#### 第一步：指令兼容（5 分钟）` | `### 第一步：指令兼容（5 分钟）` | renamed（编号去除） |
| `#### 第二步：技能共享（10 分钟）` | `### 第二步：技能共享（10 分钟）` | renamed（编号去除） |
| `#### 第三步：权限意图转换（需要理解，不能直译）` | `### 第三步：权限意图转换（需要理解，不能直译）` | renamed（编号去除） |
| `#### 第四步：逐个迁移（按优先级顺序）` | `### 第四步：逐个迁移（按优先级顺序）` | renamed（编号去除） |
| `### 3. 常见陷阱 6 条` | `## 常见陷阱 6 条` | renamed（层级 3→2，编号去除） |
| `**陷阱 1：…**` ~ `**陷阱 6：…**` | `### 陷阱 1：…` ~ `### 陷阱 6：…` | renamed（加粗段 → 标题） |
| `### 4. Skills 最佳实践 5 条` | `## Skills 最佳实践 5 条` | renamed（层级 3→2，编号去除） |
| `**实践 1：…**` ~ `**实践 5：…**` | `### 实践 1：…` ~ `### 实践 5：…` | renamed（加粗段 → 标题） |
| `### 5. 典型项目配置示例` | `## 典型项目配置示例` | renamed（层级 3→2，编号去除） |
| `**项目级 `.codex/config.toml`**：` | `### 项目级 .codex/config.toml` | renamed（加粗段 → 标题，去掉反引号） |
| `**用户级 `~/.codex/config.toml`**：` | `### 用户级 ~/.codex/config.toml` | renamed（加粗段 → 标题，去掉反引号） |
| `### 6. 迁移检查清单` | `## 迁移检查清单` | renamed（层级 3→2，编号去除） |
| `**第一步：指令兼容**` 等四个加粗段 | `### 第一步：指令兼容` 等四个子节 | renamed（加粗段 → 标题） |
| `**避坑确认**` | `### 避坑确认` | renamed（加粗段 → 标题） |
| `> **本章小结**` | `## 小结` 中的 `> [!note] 本章小结` | renamed（独立段落 → 小结节内 Callout） |
| `> [!note] 导航`（`[[07 CLI 与调试|← 上一章]] \| [[附录 快速参考卡片|附录 →]]`） | `## 相关文档` 表格 | removed → replaced |
| （无） | `## 常见问题`（3 条 FAQ，源自内容） | added |
| （无） | `## 最佳实践`（Do's / Don'ts） | added |
| （无） | `## 小结`（摘要段 + `> [!note] 本章小结`） | added |
| （无） | `## 相关文档` | added |
| （无） | `## 参考资料` | added |
| （无） | `## 更新记录` | added |

## 内容保留说明

- **代码块全部保留**：指令兼容 `config.toml`、`codex status` bash、技能共享 `ln -s` bash、权限意图转换 `text`、逐级迁移优先级 `text`、陷阱 1 的 bash、陷阱 2/3/4/5/6 的 `config.toml`、实践 1 的 yaml、项目级/用户级两份完整 `config.toml` 示例。
- **表格全部保留**：文件与路径、指令与规则、技能系统、扩展与安全、Codex 独有功能 共 5 张对照表；迁移检查清单 4 组 checklist 与避坑确认 6 项。
- **Callout 全部保留**：`> **核心结论**`、`> **本章小结**`（并入 `## 小结`）。
- **删除项**：旧的书本式导航块（`> [!note] 导航` / `上一章` / `附录`），仅移除章节交叉引用文字，未删除技术内容。

## 命名对照（wikilink 目标）

| 参数文档 | 指向文件 |
|------|------|
| `[[Codex 配置哲学概览]]` | `01 配置哲学概览.md`（原 01 章） |
| `[[AGENTS.md 分层体系]]` | `03 AGENTS.md 分层体系.md`（原 03 章） |
| `[[Skills 技能系统]]` | `04 Skills 技能系统.md`（原 04 章） |
| `[[快速参考卡片]]` | `附录 快速参考卡片.md` |
| `[[Codex MOC]]` | `Codex 配置体系 MOC.md` |
