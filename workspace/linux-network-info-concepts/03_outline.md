## 学习笔记大纲：《Linux 网络信息获取与概念》

> 笔记类型：实战笔记（概念解释 + 查询命令 + 实战示例，按主题分篇）
> 预计总篇幅：约 10 章 / 40-60 页
> 章节数：10 章

---

### 第一章：网络信息查询概览与工具链

- **篇幅**：短
- **覆盖要点**：OSI/TCP-IP 分层模型与网络信息映射、iproute2 vs net-tools 全面对比、查询命令速查表、信息查询通用思路
- **素材引用**：精读-分层模型、维度1-1（网络基础四要素）、维度1-5（基础知识）、最佳实践#1
- **代码示例**：无（本章无实操，为后续章节做概念铺垫）

---

### 第二章：网络接口与链路层信息

- **篇幅**：中
- **覆盖要点**：MAC 地址与以太网帧结构、MTU 与 IP 分片原理、`ip link` 命令详解（状态/统计/简洁模式）、`ethtool` 硬件信息与驱动查询、`ip -j` JSON 输出
- **素材引用**：维度1-2（数据链路层）、维度2-1（ip 命令）、精读-ip link、精读-ethtool、维度4-2（ethtool -S）
- **代码示例**：有

---

### 第三章：IP 地址与子网信息

- **篇幅**：中
- **覆盖要点**：IPv4 32 位结构、子网掩码与 CIDR 表示法、特殊地址（loopback/link-local/私有地址）、`ip addr` 命令详解（简洁/JSON/scope/flag）、`ip -j` + jq 脚本解析
- **素材引用**：维度1-1（IP/子网/网关/DNS）、维度1-3（排查顺序）、精读-IP 地址与子网掩码、精读-特殊地址
- **代码示例**：有

---

### 第四章：路由表信息

- **篇幅**：中
- **覆盖要点**：路由表核心概念（直连/静态/动态、默认路由、最长前缀匹配）、策略路由（`ip rule` 多路由表）、`ip route` 命令详解（show/get/default）、`ip route get` 路由决策模拟
- **素材引用**：维度1-1（路由/DNS）、维度2-4（Arch Wiki 排障）、精读-路由表、维度1-3（排查顺序）
- **代码示例**：有

---

### 第五章：DNS 解析与域名信息

- **篇幅**：长
- **覆盖要点**：DNS 解析完整流程与记录类型（A/AAAA/CNAME/MX/NS/TXT/SOA）、配置文件链路（`/etc/nsswitch.conf` -> `/etc/hosts` -> `/etc/resolv.conf`）、systemd-resolved 体系（`resolvectl` stub 127.0.0.53）、`dig` 详解（+short/+trace/@server/-x）、`nslookup` 与 `host` 快速查询、DNS 缓存操作
- **素材引用**：维度1-1（DNS）、维度2-3（DNS 排障）、维度3-2（DNS 配置指南）、维度3-3（resolvectl 手册）、精读-DNS 解析、精读-配置文件体系
- **代码示例**：有

---

### 第六章：ARP 与邻居发现

- **篇幅**：短
- **覆盖要点**：ARP 协议（广播请求/单播回复）、IPv6 NDP 替代 ARP、邻居状态机（REACHABLE/STALE/DELAY/PROBE/FAILED/PERMANENT）、`ip neigh` 命令详解（show/flush/add）、ARP 表溢出与 `gc_thresh` 排障
- **素材引用**：维度1-2（ARP 协议）、维度2-5（arp vs ip neigh）、精读-ARP 与邻居表、常见坑#6（ARP 表溢出）
- **代码示例**：有

---

### 第七章：Socket 连接与传输层信息

- **篇幅**：中
- **覆盖要点**：TCP/UDP 协议头部结构、TCP 状态机（LISTEN/ESTABLISHED/TIME-WAIT/CLOSE-WAIT）、Socket 与连接五元组、`ss` 命令详解（-tulnp/-tanp/-s/-i/状态过滤/端口过滤）、Recv-Q/Send-Q 排障、`ss` vs `netstat` 性能对比
- **素材引用**：维度2-2（ss 指南）、维度1-4（协议格式）、精读-TCP/UDP 与 Socket、维度1-5（Socket 类型）、常见坑#3（netstat 慢）
- **代码示例**：有

---

### 第八章：无线网络信息

- **篇幅**：短
- **覆盖要点**：`iw` 命令族（dev/link/scan/reg）、`iwconfig` 旧版对比、`nmcli` 无线管理（device wifi list/connect）、WiFi 信号强度与速率解读
- **素材引用**：维度4-3（iw vs iwconfig）、维度3-4（nmcli 指南）、精读-无线网络工具
- **代码示例**：有

---

### 第九章：网络监控与统计

- **篇幅**：中
- **覆盖要点**：实时带宽监控（`iftop` 按连接排序、`nload` 简洁流量图、`bmon` ASCII 图表）、进程级流量（`nethogs` 按 PID 归因）、网卡统计（`ethtool -S` CRC 错误/丢包）、历史统计（`vnstat` 日/月流量）、`/proc/net/dev` 底层原理
- **素材引用**：维度4-1（接口与带宽监控）、维度4-2（网络监控）、维度4-4（流量监控实践）、维度4-5（网络吞吐量）、精读-网络监控工具、常见坑#4（nload 单位）
- **代码示例**：有

---

### 第十章：抓包与协议分析基础

- **篇幅**：中
- **覆盖要点**：`tcpdump` 基础用法（-i/-c/-n）、BPF 过滤表达式（host/port/tcp/udp/icmp）、抓包存储与轮转（-w/-r/-C/-W）、报文输出解读（Flag/seq/ack）、权限模型（root/CAP_NET_RAW）
- **素材引用**：维度4-2（监控含 tcpdump）、精读-抓包基础、常见坑#5（tcpdump 权限）、维度1-4（协议格式辅助解析）
- **代码示例**：有

---

## 学习路径说明

### 前置要求

- 基本的 Linux 命令行操作能力（能运行命令、理解管道和重定向）
- 了解 IP 地址的基本概念（知道 IPv4 是类似 `192.168.1.1` 的数字）
- 能使用包管理器安装软件（`apt install` / `pacman -S`）

### 学完能做什么

- 熟练使用 `iproute2` 全家桶（`ip link`、`ip addr`、`ip route`、`ip neigh`）查询一切网络信息
- 理解 IP 子网划分、路由选择、DNS 解析、ARP 发现等核心概念及它们在实际系统上的表现
- 用 `ss` 替代 `netstat`，快速定位端口占用和连接异常
- 用 `dig` 和 `resolvectl` 排查域名解析故障（包括 systemd-resolved 相关痛点）
- 用 `iw` 和 `nmcli` 管理无线网络连接信息
- 用 `iftop`、`nload`、`nethogs` 监控实时带宽和进程级流量
- 用 `tcpdump` 抓包并分析基本的网络协议交互
- 理解并避开 Linux 网络查询中的常见坑（resolv.conf 被覆盖、ARP 表溢出、ICMP 限速假阳性等）

### 建议学习顺序

1. **第一章必读**：奠定分层模型和工具家族的全局认知，后续各章都基于此框架
2. **第二至七章建议按序阅读**：从 L2 到 L7 层层递进，每章概念依赖前一章（如路由依赖 IP、ARP 依赖链路层）
3. **第八章（无线）**：如果当前设备没有无线网卡，可跳读或仅了解命令结构
4. **第九章（监控）与第十章（抓包）**：属于独立进阶技能，可在前面七章之后任意顺序学习
5. **总学习时间预估**：带实操约 8-12 小时（第一章 0.5h + 第二至七章各 1-1.5h + 第八至十章各 0.5-1h）
