---
title: "显存（VRAM）与本地部署大模型：本地跑大模型先搞懂显存"
tags:
  - AI学习
  - 硬件/显卡
  - 显存
  - 大模型
  - 本地部署
  - 入门
created: 2026-08-10
updated: 2026-08-10
status: done
source_project: vram-local-llm-deployment
---

# 显存（VRAM）与本地部署大模型：本地跑大模型先搞懂显存

本地部署大模型，第一道绕不过去的坎就是[[显存]]（VRAM）。这本笔记从零基础出发，用大白话和打比方带你搞懂「显存是什么」「大模型为什么吃显存」「怎么用[[量化]]把模型塞进显存」，最后附上选卡速查表和一行命令的部署实战——读完你就能看懂显存参数，判断自己的显卡能跑多大模型。

> [!summary] 读完这篇笔记你能做到
> - 分清显存、内存、硬盘，看懂显卡参数（容量 / 带宽 / 位宽）
> - 用公式 + 速查表估算「我的显卡能跑多大模型」
> - 用 Ollama 一行命令跑起本地量化模型，并确认它在用 GPU
> - 遇到 CUDA out of memory 会按三板斧自行排障


## 目录

1. [第 1 章：显存是什么？GPU 的专属工作台](#第-1-章显存是什么gpu-的专属工作台)
2. [第 2 章：大模型为什么吃显存](#第-2-章大模型为什么吃显存)
3. [第 3 章：量化——把大模型塞进显存的钥匙](#第-3-章量化把大模型塞进显存的钥匙)
4. [第 4 章：本地部署实战——看显存、选模型、跑起来](#第-4-章本地部署实战看显存选模型跑起来)
5. [第 5 章：选卡速查表与进阶方向](#第-5-章选卡速查表与进阶方向)

---

## 第 1 章：显存是什么？GPU 的专属工作台

买电脑时你经常听到「内存 16G」「显存 8G」，它们都带个「存」，却完全不是一回事。这一章要解决一个最基础也最关键的疑问：**显存到底是什么？它和内存有什么区别？为什么显卡离了它就转不动？** 搞懂了这张「专属工作台」，你才能看懂后面几章里「大模型为什么吃显存」「我的显卡能跑多大模型」。

### 1.1 CPU 与 GPU 的分工

在讲显存之前，先分清电脑里两个「干活的人」：CPU 和 GPU。它们的脾气完全不同。

#### 1.1.1 为什么 GPU 需要一块「专属」存储

CPU（中央处理器）是电脑的总指挥，擅长处理**复杂但一步接一步**的任务：打开软件、运行程序、管理文件，都需要它。它像一位总厨，负责统筹全局、处理各种精细活。

GPU（图形处理器）则完全是另一个物种：它擅长**海量、简单、可并行的运算**。一块显卡里有几千个甚至上万个「小计算单元」，能同时算一大片数据。渲染游戏画面、训练和运行大模型，本质上都是「同一套运算对着海量数据重复做无数遍」——这正是 GPU 的主场。它像一支炒锅队，几十口锅同时开火。

> [!tip] 大白话
> 把 CPU 想成餐厅总厨，把 GPU 想成一支炒锅师傅团队。总厨一个人要处理点菜、摆盘、协调等复杂杂事；炒锅队只做一件事——同时翻炒几百份菜。炒锅队火力猛、翻得快，但只会这一件事。

因为 GPU 干活时每一刻都在高速吞吐海量数据，它不能慢悠悠地去跟 CPU 抢系统内存用。如果几十口锅都跑去同一个大仓库取食材，仓库门口立刻堵死。所以 GPU 需要一块**离自己最近、速度最快、谁也抢不走的专属存储**。

#### 1.1.2 显存：焊在显卡上、与 GPU 直连的高速存储

这块专属存储，就是**显存（VRAM，Video Random Access Memory）**。它是专为 GPU 设计、与 GPU 核心直连的高速存储，直接焊在显卡上，是显卡的一部分。你买显卡时看到的「8GB」「12GB」「24GB」，指的就是这块存储的容量。

一句话记住它的地位：**显存是 GPU 的工作台，GPU 只能在工作台上干活。** 后面你会反复看到，这个「只能」是理解大模型部署一切问题的钥匙。

### 1.2 显存 vs 内存：一场「不可互换」的误会

很多人以为显存和内存差不多，缺了显存就加内存条——这是本笔记要纠正的第一个、也是最大的误会。它俩服务对象不同、位置不同、价格不同，**不可互换**。

#### 1.2.1 服务对象 / 容量 / 带宽 / 位置 / 成本对照表

先看一张对照表（带宽的概念 1.3 会细讲，这里先感受数量级）：

| 维度 | 内存（RAM） | 显存（VRAM） |
|------|-------------|--------------|
| 服务对象 | CPU | GPU |
| 物理位置 | 插在主板上，可自行加装升级 | 焊在显卡上，与 GPU 直连 |
| 典型容量 | 大（16 / 32 / 64 GB 常见） | 相对小（消费级常见 8 / 12 / 16 / 24 GB） |
| 带宽量级 | 低（DDR5 约 50-100 GB/s） | 高（GDDR6X/7 约 500-1000 GB/s，HBM3 可达 2-5 TB/s） |
| 设计目标 | 大容量、低延迟、便宜 | 高带宽、高吞吐、贵 |
| 成本 | 低 | 高（HBM 每 GB 成本可达内存的百倍） |

内存是 CPU 的「中央备料台」，容量大、便宜，谁都能用；显存是 GPU 的「猛火灶台」，离炒锅最近、火力最猛，但只给炒锅队用。GPU 求的是高带宽——一秒能灌进多少数据；CPU 求的是低延迟——发出指令后多久能拿到结果。目标不同，造出来的存储自然也不同。

> [!tip] 大白话
> 把内存想成厨房中央的备料台，显存想成每个炒锅师傅手边的猛火灶台。备料台很大、放得下所有食材，但离得远、取料慢；灶台离得近、火又旺，可地方小、只放得下当前要炒的菜。CPU 用备料台，GPU 用灶台，两者不能互相顶替。

#### 1.2.2 为什么「加内存条」救不了显存不足

核心规则只有一句：**GPU 只能读显存里的数据计算，内存再大也只是中转仓库。** 数据先要放到显存里，GPU 才算得动；放在内存里，GPU 够不着。

这就是为什么游戏或 AI 程序报 `CUDA out of memory` 时，你插再多内存条都没用——报错说的是**显存**不够，不是内存不够。举个真实场景：一台 32GB 内存 + 8GB 显存的电脑，加载一个约 14GB 的模型，照样报 CUDA OOM，因为那 14GB 必须装进 8GB 的显存里，32GB 内存帮不上忙。加内存条救不了显存不足，这是本地跑大模型被坑最多的一课。

### 1.3 显存四大参数：容量、频率、位宽、带宽

看显卡参数表，你总会碰到这四个词：**容量（GB）、频率（MHz）、位宽（bit）、带宽（GB/s）**。前三个是「原料」，第四个是「成品」——带宽才是真正决定显卡快慢的核心指标。

> [!tip] 大白话
> 把显存系统想成一座水站。容量 = 水池大小，决定能存多少水；频率 = 进水管的水流速度，决定水跑得多急；位宽 = 水管直径，决定一次能并排进多少水；带宽 = 进水总速度，是最终「每秒能灌进多少水」的合体结果。

#### 1.3.1 带宽 = 频率 × 位宽 ÷ 8

带宽的公式是显存知识里最值得记住的一个式子：

```text
带宽（GB/s）= 频率（MHz） × 位宽（bit） ÷ 8
```

拆开看三个部件：

- **容量（GB）**：工作台能摆多少数据，决定「装不装得下」。
- **频率（MHz）**：显存一秒钟读写多少次，像进水管的水流速度。
- **位宽（bit）**：一个时钟周期能传多少位数据，相当于水管直径或马路宽度。消费级显卡常见位宽梯度是 128 / 192 / 256 / 384 bit。
- **带宽（GB/s）**：最终每秒能传多少数据，是「频率 × 位宽」合起来的效果。

末尾「÷8」是因为 8 bit = 1 Byte，而带宽习惯用字节（GB/s）表示。

> [!tip] 大白话
> 把带宽想成高速公路的车流量。位宽 = 车道数（8 车道比 4 车道能同时跑更多车），频率 = 限速（每辆车跑多快），带宽 = 每小时实际通过的车总数。想多通车，多修车道和提速都有效，缺一不可。

一个常见误区：**显存频率 ≠ GPU 核心频率**，两个「频率」是完全不同的东西。另一个容易忽略的点：位宽一旦缩水，代价很大——位宽从 256 bit 降到 128 bit，显存带宽性能大约下降 32%。

#### 1.3.2 落地例子：RTX 4090 ≈ 1TB/s、RX 7900XTX ≈ 960GB/s

公式背下来之后，看两个真实例子把它落地：

- **RTX 4090**：高频 GDDR6X 显存 × 384 bit 位宽 ÷ 8 ≈ **1 TB/s** 带宽。
- **RX 7900 XTX**：20 Gbps × 384 bit ÷ 8 ≈ **960 GB/s** 带宽。

再对比内存：CPU 的内存位宽常年是 64 bit。位宽差了数倍、频率又低，所以显存带宽能比内存高出**数十倍**——这也是 GPU 必须配专属显存的原因之一。

### 1.4 容量 ≠ 性能：够用即可

容量和带宽，一个管「装不装得下」，一个管「跑得快不快」。**显存大不代表显卡强**，容量和性能并不成正比。

一个经典反例：RTX 3060 有 12GB 显存，RTX 3080 只有 10GB，但 3080 整体性能明显更强——因为它的带宽、核心算力全面占优。反过来，如果容量不足，数据装不下，就会爆显存、掉帧卡顿；容量过剩，多出来的部分也不会让性能再提升。

> [!tip] 大白话
> 把显存容量想成行李箱的大小。箱子大，只代表能装更多东西，不代表你走得快；走得快不快，取决于你的腿脚（带宽）和体力（GPU 核心）。买显卡别只盯着箱子多大。

#### 1.4.1 分辨率参考：1080P ≈ 6GB / 2K ≈ 8GB / 4K ≥ 12GB

不同负载对显存容量的需求差很远。以游戏渲染为例，一个粗略参考：

| 负载 | 显存参考 |
|------|----------|
| 1080P 游戏 | 约 6GB |
| 2K 游戏 | 约 8GB |
| 4K 游戏 | 12GB 起步 |

这是游戏场景的经验参考值：4K 分辨率画面数据量大，需要更大的「工作台」。注意，这仅仅是游戏渲染的参考——**跑大模型是另一套算法**，需要的显存由参数量、精度、上下文长度决定，第 2 章专门讲。

#### 1.4.2 爆显存（崩溃报错）vs 爆内存（整机卡顿）

容量不够时会「爆」，但爆显存和爆内存的症状完全不同，很好区分：

| 情况 | 表现 |
|------|------|
| 爆显存 | 正在跑的程序**中途崩溃、弹报错**（如 `CUDA out of memory`、显存不足），但电脑系统本身还能用 |
| 爆内存 | **整机变卡顿**，系统提示内存不足，其他程序也跟着遭殃 |

记住这个区别，以后看到报错就能立刻判断问题出在显存还是内存，不会一通乱加硬件。

### 1.5 显存家族扫盲：GDDR vs HBM

显存不是一个统一的东西，按用途分两大「家族」：面向普通消费级的 GDDR，和面向数据中心/专业卡的 HBM。

| 家族 | 定位 | 代表型号 | 位宽 | 带宽量级 |
|------|------|----------|------|----------|
| GDDR | 消费级显卡主流 | GDDR5 / 6 / 6X / 7 | 128-384 bit | 约 500-1000 GB/s |
| HBM | 数据中心、专业计算 | HBM2 / HBM3 | 1024 bit 起步 | 可达 2-5 TB/s |

- **GDDR 家族**：靠「高频率 + 大位宽」硬堆带宽，是游戏卡和消费级 AI 卡的绝对主力。代际演进明显：GDDR5 → GDDR6 → GDDR6X → GDDR7，每一代都在提频提速（GDDR7 还引入了 PAM-3 信号技术）。
- **HBM 家族**：采用 3D 堆叠 + 硅通孔（TSV）技术，把多层存储像叠高楼一样叠起来，用极宽的接口取数据，位宽 1024 bit 起步，带宽轻松破 TB/s。代价是贵——每 GB 成本可达内存的百倍，所以几乎只出现在高端专业卡和数据中心。

对我们普通用户，一句话建议：**消费级显卡认准 GDDR 就行，不必纠结 HBM**。如果你的目标是本地跑大模型，入门建议选 **12GB 显存起步**，这正好呼应第 4 章的选卡速查表。

---

**本章小结**

- 显存（VRAM）是焊在显卡上、与 GPU 直连的专属高速存储；GPU 只能读显存里的数据，内存再大也只是中转仓库。
- 显存与内存不可互换：服务对象、位置、带宽、成本都不同，显存带宽可超内存数十倍。
- 显存四大参数：容量、频率、位宽、带宽；核心公式是 **带宽 = 频率 × 位宽 ÷ 8**。
- 容量决定「装不装得下」（1080P ≈ 6GB / 2K ≈ 8GB / 4K ≥ 12GB），带宽决定「快不快」；显存够用即可，不是越大越好。
- 爆显存是程序崩溃报错，爆内存是整机卡顿，两者症状完全不同。
- GDDR 是消费级主流，HBM 是数据中心级；本地跑大模型入门建议 12GB 起步。

下一章，我们把这把「工作台」和 AI 接上头：为什么一个开源大模型动辄要吃十几个 GB 的显存？权重、精度、KV Cache——第 2 章《大模型为什么吃显存》会把这张「显存账单」一笔一笔拆给你看。

---

## 第 2 章：大模型为什么吃显存

上一章认识了显存这张「GPU 专属工作台」。这一章来解决真正让你头疼的问题：为什么一个开源大模型，下载才几个 GB，跑起来却要吃掉十几 GB 甚至几十 GB 显存？答案是一张**显存账单**——它不是一整笔巨款，而是两栏：**权重（固定月租）+ [[KV Cache]]（按量计费）**。看懂这两栏，你就掌握了本地跑模型的核心心法。

### 2.1 模型为什么必须装进显存才能跑

先回到第 1 章那条铁律：**GPU 只能读显存里的数据计算。** 大模型本质上是一张巨大的「权重表」，模型每次回答都要对着这张表反反复复做乘法运算。权重表放在哪，直接决定了 GPU 干活快慢：

- 放在**显存**里：GPU 随手就能取，算得快。
- 放在**内存**里：GPU 够不着，只能靠 CPU 慢吞吞地搬运，速度慢几十上百倍，几乎不可用。

所以「模型必须装进显存才能跑」不是偏好，是硬约束。装不装得下，就成了本地部署的第一道门槛。

### 2.2 权重：模型的「固定身价」

权重（weights）是模型「学会的知识」，也是占显存的大头。它的特点是：**不管你有没有在对话，只要模型一加载，这部分显存就先被占掉了。** 就像房租，不住人也得按月付。所以我们叫它「固定月租」。

> [!tip] 大白话
> 把大模型的显存账单想成手机话费。权重 = 固定月租，只要套餐在就天天扣，不管你这月打没打电话；KV Cache = 按量计费的流量，用多少算多少。看本地部署够不够显存，先把月租这项算清楚，再估流量。

#### 2.2.1 参数量：7B = 70 亿个参数

权重大小由两件事决定，第一件是**参数量**。参数（parameter）是模型里一个个可调整的「旋钮」，模型的智能就藏在这几十亿个旋钮的取值里。7B = 70 亿（Billion）个参数，30B、70B 同理。参数量越大，模型越「聪明」，但要装的显存也越多。

> [!tip] 大白话
> 把模型想成一本超大的词典，每个参数是一个词条。词典越厚（参数量越大），能解释的词越多、越聪明，但这本词典本身就占地方。注意：这里只说了「词典有多厚」，每个字用几格墨水来写，是下一小节的事。

#### 2.2.2 精度：FP32/FP16/INT8/INT4 每参数占几个字节

决定权重占多少显存的第二件事，是**精度**——每个参数用几个字节来存。同样的参数，存得越「精细」占得越多：

| 精度 | 每参数字节数 | 直观叫法 |
|------|-------------|----------|
| FP32 | 4 字节 | 全精度 |
| FP16 / BF16 | 2 字节 | 半精度（消费级标准） |
| INT8 | 1 字节 | 8bit 量化 |
| INT4 | 0.5 字节 | 4bit 量化 |

> [!tip] 大白话
> 精度决定每个词条用几格墨水来写。FP32 用 4 格，写得最精细；FP16 用 2 格；INT4 只用半格，字迹潦草但省纸。同一本词典，用不同精度写，体积差好几倍。所以 7B 模型用 FP16 要 14GB，用 INT4 只要 3.5GB——这就是第 3 章「量化」的威力。

#### 2.2.3 估算公式：权重显存 = 参数量 × 每参数字节数

把两件事乘起来，就得到权重显存的估算公式：

```text
权重显存（GB）= 参数量 × 每参数字节数
```

注意单位换算：参数量是「个」，每参数字节数是「字节」，乘出来是字节；要除以 10 亿才换算成 GB（10 亿字节 ≈ 1GB）。

#### 2.2.4 动手算：7B FP16 ≈ 14GB、32B ≈ 60GB

拿公式算两个最常见的例子：

- **7B FP16**：70 亿 × 2 字节 = 140 亿字节 ≈ **14GB**。
- **32B FP16**：320 亿 × 2 字节 = 640 亿字节 ≈ **60GB**。

看到差距了吗？一个 7B 模型的「固定月租」就是 14GB——这已经超过很多人的 8GB 显卡总显存了。这也解释了第 1 章里「32GB 内存 + 8GB 显存照样报 CUDA OOM」：那 14GB 权重必须住进 8GB 的显存，内存再大也无济于事。

### 2.3 KV Cache：每次对话的「临时便签」

权重是固定月租，但模型一旦开始「聊天」，就会产生第二笔开销：**KV Cache**。它像每次对话临时贴上的便签，随聊随长、随增随占，所以是「按量计费」。

#### 2.3.1 为什么需要它（以存代算，避免重复计算）

模型生成回答是「一个字一个字往外蹦」的。每生成一个新字，模型都要重新「看一眼」已经生成过的整段内容。如果不缓存，生成 100 个字，前面每个字都要被反复重算，总计约 5050 次计算；有了 KV Cache，把算过的 Key 和 Value 存下来，新字只需接着算一次，总共只要 100 次。

一句话：**KV Cache 是以存代算**——花一点显存，换掉大量重复计算，否则模型慢到没法用。

> [!tip] 大白话
> 把 KV Cache 想成读书时的书签。没有书签，每次读到哪了都要从头翻一遍；插上书签，直接翻到上次的位置接着读。每轮对话都会多插一个书签，对话越长、书签越多，占的书页（显存）也越多。

#### 2.3.2 随上下文长度 × 并发线性增长

KV Cache 的大小由两个因素相乘决定：**上下文长度**（一次对话多长）和**并发数**（同时几个对话）。两者都是「每涨一点、显存线性涨一点」。

一个直观例子（Llama-3 70B 级别）：上下文 4K 时，每个请求约 160MB；上下文拉到 32K，每个请求约 1.28GB——翻了 8 倍。如果同时有几十个请求在跑，KV Cache 会迅速膨胀，甚至反超权重成为显存大头。

> [!tip] 大白话
> 把显存想成一个停车场。权重是几辆常驻大车，一直占着固定车位；KV Cache 是每来一个请求就多停一辆的小车。上下文越长，每辆车越大；并发越高，车越多。车位（显存）是固定的，车一多必然停不下。

#### 2.3.3 长上下文为什么是显存杀手

上下文窗口（context window）是模型一次能「看到」多少内容的能力，常被厂商当卖点宣传：32K、128K 甚至更多。但窗口开多大，KV Cache 就线性涨多大——**「支持长上下文」和「长上下文不占显存」是两回事**。

实际部署时，把上下文参数从 2048 拉到 4096、8192，KV Cache 跟着翻倍涨。很多人一上来把上下文拉满，结果 12GB 的卡直接被撑爆，报错 `CUDA out of memory`。这几乎是本地部署第一大坑。

> [!note] 进阶脚注
> 业界也在解决 KV Cache 膨胀：NVIDIA 的 NVFP4 技术把 KV Cache 压到 4bit，显存比 FP8 再降约 50%，同样显存能容纳的上下文容量翻倍（面向 Blackwell 数据中心卡，消费级暂用不上）。这是后话，入门阶段知道「KV Cache 是线性增长」就够。

### 2.4 推理还有哪些开销：激活值 + 框架开销 + 安全系数

权重 + KV Cache 是两大主力，但完整账单还有三笔零头，加起来也不能忽略：

- **激活值（activations）**：前向计算时层与层之间传递的中间张量，随模型跑动临时产生。简化估算约为权重的 10%（7B 模型、2048 上下文，约 1.4GB）。
- **框架开销**：CUDA 驱动、推理引擎本身也要占 1-2GB 显存。
- **安全系数**：估算只是起点，显存会被系统悄悄吃掉一点（比如 24GB 卡实际可用约 22GB），所以建议按一定比例预留余量。

推理的完整估算式可以写成：

```text
推理显存 ≈（权重 + KV Cache + 激活值）× 安全系数 + 框架开销
```

| 组成 | 特点 | 估算参考 |
|------|------|----------|
| 权重 | 固定月租，加载即占 | 参数量 × 每参数字节数 |
| KV Cache | 按量计费，随上下文 × 并发涨 | 上下文越长、并发越高越大 |
| 激活值 | 计算时临时产生 | 简化约权重的 10% |
| 框架开销 | 引擎本身占用 | 约 1-2GB |
| 安全系数 | 兜底余量 | 推理建议 1.2-1.5 倍 |

### 2.5 顺带一提：训练 vs 推理（为什么训练更吃显存）

前面算的都是「推理」——用现成模型回答问题。如果是「训练」——教模型学习，账单会暴涨好几倍。差别在于训练除了权重，还要额外保存：

- **梯度（gradient）**：每次更新权重前的「草稿」，告诉模型往哪个方向调。
- **优化器状态（optimizer state）**：Adam 等优化器为每个参数额外保存的「账本」，每参数约 8 字节。

> [!tip] 大白话
> 把训练想成做一道超难的数学题。推理只是抄答案（用权重），训练要从头推导：既要写过程草稿（梯度），又要记账对账（优化器状态），占的地方自然比「只看答案」多好几倍。LoRA、QLoRA 这类微调技巧，就像只对答案的最后一页做小改动，不用重写整本书，所以省显存。

用官方数字对比最直观（7B 模型）：

| 任务 | 大致显存需求 |
|------|-------------|
| 推理（FP16 权重） | 约 14GB |
| 全参数微调 | 约 87GB |
| LoRA 微调 | 约 18GB |
| QLoRA（4bit）微调 | 约 7.6GB |

这就是为什么训练通常是多卡数据中心的活，而消费级显卡一般只做推理；就算微调，也要优先选 LoRA/QLoRA 这类省显存的方案。

---

**本章小结**

- 大模型必须整体装进显存才能跑，因为 GPU 只能读显存里的数据。
- 显存账单有两栏：**权重（固定月租）+ KV Cache（按量计费）**。
- 权重显存 = 参数量 × 每参数字节数；7B FP16 ≈ 14GB、32B FP16 ≈ 60GB。
- KV Cache 以存代算，随上下文长度 × 并发线性增长，长上下文是显存杀手。
- 完整推理账单 =（权重 + KV Cache + 激活值）× 安全系数 + 框架开销。
- 训练比推理多吃「梯度 + 优化器状态」，7B 全参微调约 87GB，LoRA/QLoRA 可大幅降低。

下一章，面对「原生 FP16 的 7B 模型就要 14GB，普通显卡装不下」这道门槛，我们祭出那把钥匙——**量化**：把大模型像行李一样压缩打包，让它在显存里安家。

---

## 第 3 章：量化——把大模型塞进显存的钥匙

第 2 章算完账，你会发现一个尴尬：原生 FP16 的 7B 模型就要 14GB，32B 更要 60GB——大多数人的显卡根本装不下，连跑都跑不起来。这一章介绍那把钥匙：**量化（quantization）**。它不是魔法，而是「用少量精度换大量显存」的打包术，也是本地部署圈最常用的一招。

### 3.1 为什么需要量化（装不下的问题）

回到第 2 章的公式：**权重显存 = 参数量 × 每参数字节数**。装不装得下，是个硬门槛——装不下，模型就完全跑不了，跟「慢」不是一个量级的问题。

面对这道门槛，只有两条路：

- **换更大的显卡**：贵，而且消费级顶配（24GB）也装不下原生 60GB 的 32B 模型。
- **把模型压缩**：让同样一个模型，用更少的字节装进显存——这就是量化。

普通人想本地跑大模型，基本都走第二条路。

### 3.2 量化是什么：精度与压缩比

量化的原理一句话：**把每个参数的存储精度降下来。** 参数还是那些参数，知识还在，只是每个参数从 2 字节减到 1 字节甚至 0.5 字节。

#### 3.2.1 FP16 → INT8 → INT4（2B → 1B → 0.5B）

按第 2 章的精度表，把 FP16 作为基准，逐级往下压：

| 精度 | 每参数 | 相对 FP16 | 7B 模型权重 |
|------|--------|-----------|-------------|
| FP16（不量化） | 2 字节 | 1×（基准） | 约 14GB |
| INT8 | 1 字节 | 1/2 | 约 7GB |
| INT4 | 0.5 字节 | 1/4 | 约 3.5GB |

同样是 7B 模型：14GB → 7GB → 3.5GB，体积一路砍到 1/4。这就是量化「用少量精度换显存减半以上」的由来。

> [!tip] 大白话
> 把模型想成要上飞机的行李。FP16 是原封不动的粗打包，INT8 是轻轻压缩，INT4 是高压 zip 袋——衣服还是那几件，体积却小了 3/4。代价是打包要花点力气，到地方拆包也得费点劲（速度略降）。再往上还有双量化，相当于压缩袋再抽一遍真空，能在 4bit 基础上再挤一点空间（比如 32B 模型大约再省 1.5GB）。

#### 3.2.2 精度损失：Q4 能力下降约 1-3%，严肃使用建议 Q5_K_M

压缩不是免费的，代价是**精度损失**。4bit 量化（Q4）相对 BF16，模型能力大约下降 1-3%。这个幅度日常聊天、写文案基本感觉不出来；但如果用于严肃场景——专业写作、复杂编程、数据分析——就建议升到 Q5_K_M 档位，用稍多的显存换更稳的质量。

> [!tip] 大白话
> 量化损失想成保存照片时的质量选择。存 100% 质量，文件巨大；存 80%，肉眼几乎看不出差别，文件小一大截；存 30%，能看出糊了。Q4 大约就是「80%」——平时够用，放大了才露馅；要冲印大图（严肃使用），就选更高档位。

### 3.3 常见量化档位（Q4_K_M / Q8）与压缩率直觉

量化档位很多，入门记住两个就够：**Q4_K_M** 和 **Q8**。

- **Q4_K_M**：4bit 量化的主流档位。名字里的 K 表示用更精细的分组方式（k-quants）保留更多信息。效果/速度均衡，社区默认首选。
- **Q8**：8bit 量化。质量非常接近原版，但体积大约是 Q4 的两倍。

同一 7B 模型，两种档位对比：

| 档位 | 7B 权重 | 相对体积 | 特点 |
|------|---------|----------|------|
| FP16（不量化） | 约 14GB | 1× | 质量最好，显存最大 |
| Q8 | 约 8GB | 约 1/2 | 质量接近原版 |
| Q4_K_M | 约 4GB | 约 1/4 | 效果/速度均衡，社区首选 |

档位再往下（如 Q3_K_M），体积更小但质量下降明显，一般只有显存实在不够时才用。判断一个量化模型装不装得下，看文件名后缀就行——GGUF 这类模型封装格式，后缀就标着档位，比如 `q4_K_M`。

### 3.4 关键提醒：量化后权重还要 + KV Cache + 开销 + 安全系数

本地部署新手最容易掉进的坑：**把「量化后权重显存」当成「实际总需求」。** 量化只压缩了权重这一项，第 2 章那张账单的其他项目——KV Cache、激活值、框架开销、安全系数——一分不少。

```text
推理显存 ≈（量化后权重 + KV Cache + 激活值）× 安全系数 + 框架开销
```

举个实际例子：7B 模型的 Q4_K_M 权重约 4GB。可它一旦跑起来，还要加上 KV Cache（2048 上下文就约 1-2GB）、激活值、约 1-2GB 的框架开销，再乘一个安全系数——实际需求远不止 4GB。这也是为什么有人看到「权重才 4GB」却照样 `CUDA out of memory`。

> [!tip] 大白话
> 量化想成打包行李去旅行。你把衣服压进真空袋，衣服确实从一大包变成一小包（权重 14GB→4GB），但行李箱本身的重量、洗漱包、电脑（KV Cache、框架开销、安全系数）一样都少不了。压扁了衣服，不等于整个行李箱就轻到只剩衣服。

---

**本章小结**

- 量化 = 用少量精度换显存减半以上：FP16（2B/参数）→ INT8（1B）→ INT4（0.5B），同一 7B 从 14GB 降到 3.5GB。
- Q4_K_M 是 4bit 主流档位，效果/速度均衡，社区默认首选；同一 7B：Q4_K_M ≈ 4GB vs Q8 ≈ 8GB。
- 代价是精度损失：Q4 相对 BF16 能力下降约 1-3%，严肃使用建议 Q5_K_M。
- 关键提醒：**量化后权重 ≠ 实际总需求**，还要 + KV Cache + 激活值 + 框架开销 + 安全系数。
- 双量化（抽真空）能在 4bit 基础上再省一点，属进阶技巧。

下一章进入动手环节：怎么查自己显卡的显存、怎么判断能跑多大模型、怎么用 Ollama 一行命令把量化模型在本地跑起来——遇到显存不足，还有一套排障三板斧等着你。

---

## 第 4 章：本地部署实战——看显存、选模型、跑起来

前三章我们把概念铺完了：显存是什么、大模型为什么吃显存、量化怎么把模型压缩。这一章全部变成动手操作，目标很具体——**查自己显卡有多少显存 → 判断能跑多大模型 → 用 Ollama 一行命令跑起来 → 遇到显存不足能自己排障**。所有命令都能直接照抄。

### 4.1 动手前：nvidia-smi / GPU-Z 看显存

部署前第一件事，先看清自己手里有什么牌：显卡型号、总显存、现在被占了多少。

NVIDIA 显卡在终端里输入 `nvidia-smi`：

```bash
nvidia-smi
```

输出顶部能看到显卡型号、驱动版本和显存信息，其中关键一行是「Memory-Usage」，格式类似：

```text
| NVIDIA GeForce RTX 4090        |
|    22GB / 24GB                 |
```

- 左边是当前已用显存，右边是总显存。
- 注意：**总显存 ≠ 全部可用**。系统会悄悄占用一部分，比如 24GB 的 4090 实际可用约 22GB。估算模型显存时，别把 24 当满打满算。

Windows 上不想敲命令，也可以用 **GPU-Z** 这类图形工具，第一行直接显示显卡型号和显存大小，适合看入门信息。

> [!tip] 大白话
> 把 `nvidia-smi` 想成显卡的仪表盘。总显存 = 油箱总容量，已用显存 = 当前油量，剩余可用的那格油才是你能跑的。估算前先看看油表，别一上来就按满箱算。

### 4.2 我的显卡能跑多大模型

知道显存后，下一个问题是：它装得下多大的模型？

#### 4.2.1 估算公式：参数量 × 位宽 ÷ 8 × 1.2

实战圈流传一条快速估算公式，把第 2、3 章的知识浓缩成一行：

```text
所需显存（GB）≈ 参数量（B）× 位宽 ÷ 8 × 1.2
```

拆开看：

- **参数量（B）**：模型有多少亿参数，如 7B、14B。
- **位宽**：按精度填，FP16 用 16、INT8 用 8、Q4/INT4 用 4。这就是「每参数字节数」的 bit 表达（÷8 转成字节）。
- **× 1.2**：留约 20% 余量，覆盖 KV Cache、框架开销和安全系数。

动手算三个常见档位：

```text
7B  Q4：7  × 4  ÷ 8 × 1.2 ≈ 4.2GB  → 约 4GB
14B Q4：14 × 4  ÷ 8 × 1.2 ≈ 8.4GB  → 约 8GB
27B Q4：27 × 4  ÷ 8 × 1.2 ≈ 16.2GB → 约 15-16GB
```

一个更快的「心算口诀」：每 10 亿参数，BF16 约 2GB、FP8 约 1GB、INT4 约 0.5GB。记住这个比例，平时不用掏计算器。

#### 4.2.2 8 / 12 / 16 / 24 / 32GB 显存档位速查表

把公式套到常见显卡显存上，得到一张「我的显卡能跑啥」速查表（Q4 量化档为主）：

| 显存 | 参考模型（量化档） | 说明 |
|------|--------------------|------|
| 8GB | Gemma3-4B Q4 ≈ 5GB；Qwen3-8B Q4 ≈ 7.5GB | 入门门槛，只能跑小模型 |
| 12GB | Qwen3-8B Q8 ≈ 11.5GB；Phi-4 14B Q4 ≈ 9.9GB | 跑 7-8B 很从容 |
| 16GB | Qwen3-14B Q4 ≈ 12GB；DeepSeek R1 14B Q8 ≈ 16GB | 多数人的甜点档 |
| 24GB | Qwen3-32B Q4 ≈ 23GB | 可跑 30B 级（如 RTX 4090） |
| 32GB | Qwen3-32B Q8 完整装入 | 27B-32B 从 Q4 升到 Q8，质量更好 |

读这张表的两个要点：

- **16GB 是多数人的甜点档**：能舒服地跑 14B 级模型，兼顾质量和速度。
- **显存够时，优先升量化档，而不是换更大的模型**：32GB 比 24GB 多的价值，主要是让 27B-32B 从 Q4 升到 Q8，而不是硬塞 70B。

> [!tip] 大白话
> 选量化档位就是在选压缩率，和第 3 章的行李打包是同一件事：Q8 是轻压缩、Q4 是高压 zip。显存够用了，就别急着换更大的行李箱（换大模型），先把同箱行李从高压袋换回轻压缩，质量立马上一个台阶。

### 4.3 Ollama 一条命令上手

估算完心里有数，就可以动手跑了。**[[Ollama]]** 是目前最省事的一键部署工具：去官网下载对应系统的安装包装好，命令行里两条命令就能拉模型、跑模型。

#### 4.3.1 安装与 pull（注意 tag 带 q4_K_M，避免拉到 FP16）

拉取模型用 `ollama pull`：

```bash
# 基本拉取命令
ollama pull qwen:7b
```

Ollama 的模型用「模型名:标签」表示，**标签里带 `q4_K_M` 的就是 4bit 量化版**。这里有一个新手最容易踩的坑：不指定标签时，部分模型默认拉的是未量化（FP16）原版，7B 直接 14GB，小显存立刻 OOM。所以拉取前先在模型库页面确认带 `q4_K_M` 的标签，格式类似 `模型名:版本-q4_K_M`，再按这个标签拉：

```bash
# 推荐：显式指定带 q4_K_M 的量化版标签
# 具体标签名以模型库页面为准
ollama pull 模型名:版本-q4_K_M
```

> [!tip] 大白话
> Ollama 像点外卖。`ollama pull` 就是下单，系统自动把模型下载、环境、运行依赖一起打包好送到你面前，你只管「吃」。但下单时一定要看清套餐（tag）：不选量化套餐，可能给你上一份 14GB 的「全家桶」，小胃（小显存）直接撑爆。

#### 4.3.2 run 与 ollama ps 看 /GPU 字段

拉下来后运行：

```bash
# 跑起来（如果还没拉过，会自动先拉取）
ollama run qwen:7b
```

进入交互界面后直接打字就能对话。想让模型在后台运行、需要开一个终端查它到底跑在哪，用 `ollama ps`：

```bash
ollama ps
```

输出是一个表格，关键看最后一列**处理器（PROCESSOR）字段**：

- `/GPU`：模型跑在显卡上，正常。
- `/CPU`：模型没跑在显卡上（多半是显存不够被降级到 CPU），速度慢 10-100 倍，需要排查。

> [!note] 提示
> 如果 `ollama ps` 显示 `/CPU`，最常见的三个原因：显存装不下、上下文开太大、tag 拉错了。逐个用下面的三板斧排查。

### 4.4 llama.cpp 进阶路线（可选）

Ollama 底层其实就是 **llama.cpp**。想更精细地控制「模型哪些层放 GPU」，可以直接用 llama.cpp。它通过 `-ngl` 参数控制放进 GPU 的层数。

#### 4.4.1 -ngl 控制 GPU 层数

```bash
# -ngl 后面是放进 GPU 的层数，例如 33
llama-cli -m model.gguf -ngl 33
```

- `-ngl`（全称 `--n-gpu-layers`）告诉引擎「把前 N 层放进 GPU」。
- 显存充足就调大 `-ngl`，让更多层用 GPU 加速；显存不足就调小，把更多层留给 CPU。

#### 4.4.2 offload 到 CPU：慢但能跑

当显存不够时，llama.cpp 不会直接崩溃，而是把放不下的层**卸载（offload）到 CPU 内存**里跑。速度明显变慢，但至少能跑——这是「能跑」和「跑不动」之间的关键缓冲。例如一台 8GB 显存的笔记本，partial offload 也能跑起 32B Q4_K_M，只是速度掉到每秒约 10 个 token 的量级（能对话，但不算流畅）。

### 4.5 OOM 排障三板斧

遇到 `CUDA out of memory` 别慌，这是本地部署最常见的报错。按顺序试三板斧，大部分情况都能解决：**降上下文 → 降量化档 → offload / 换更小模型**。

#### 4.5.1 降上下文（--num-ctx 2048/1024）

第一板斧，也是见效最快的一招：把上下文长度调小。KV Cache 随上下文线性增长（第 2 章讲过），调小它立刻释放一大块显存。

```bash
# Ollama 运行时指定小上下文
ollama run qwen:7b --num-ctx 2048
```

把上下文从默认的大窗口降到 2048，还不够就 1024。Ollama 也可以用环境变量 `OLLAMA_CONTEXT_LENGTH` 统一设置。

> [!tip] 大白话
> 把上下文想成教室里的临时黑板。黑板越大，能同时写的字越多，但占的墙面（显存）也越多。写不下了，就把黑板换小一号——先让课讲起来，黑板大了写得多只是锦上添花。

#### 4.5.2 降量化档位

第二板斧：把模型从高精度换到更低精度。Q8 换成 Q4_K_M，权重显存直接减半；再不行从更大模型换小模型的 Q3 档。Q4_K_M 是质量/体积的平衡点，再往下（如 Q3）质量下降就比较明显了，所以优先在 Q8 和 Q4_K_M 之间切换。

#### 4.5.3 offload / 换更小模型

最后一板斧，两个动作：

- **offload**：用 llama.cpp 的 `-ngl` 减小 GPU 层数，把部分层卸到 CPU（4.4.2）。
- **换更小模型**：14B → 7B → 4B。记住那句口诀——显存不够时先降档、再换小；显存够用时优先升档而不是换大。

三板斧的完整顺序，就是从上到下、从「损失最小」到「损失最大」，每一步都先试试再说。

### 4.6 实战常见坑汇总

把上面散落的坑集中成一张表，排障时对照着看：

| 坑 | 现象 | 解法 |
|----|------|------|
| 内存冒充显存 | 模型被装进 CPU 内存而非显存，慢 10-100 倍 | `ollama ps` 看是否 `/GPU`，不是就排查 |
| 选错量化档位 | `ollama pull` 没指定 tag 拉到 FP16，7B 直接 14GB | 拉带 `q4_K_M` 的标签 |
| 上下文开太大 | 默认大上下文把 12GB 卡撑 OOM | 降 `--num-ctx 2048/1024` |
| 显存被系统吃掉 | 24GB 卡实际可用约 22GB | 估算留余量，别按满显存算 |
| 被 MoE 文件体积骗了 | 16B MoE 下载才 6GB，实际显存要 23GB | MoE 看总参数，别只看下载体积 |
| vLLM + bitsandbytes 不兼容 | 进阶场景报量化加载错 | 设 `VLLM_USE_V1=0`（进阶用到再说）|

最后一条涉及 vLLM，属于第 5 章进阶内容，这里记个印象即可：遇到再回来翻。

---

**本章小结**

- `nvidia-smi` 查显存，注意总显存 ≠ 全部可用（24GB 实际约 22GB）。
- 估算公式：**所需显存 ≈ 参数量 × 位宽 ÷ 8 × 1.2**；7B Q4 ≈ 4GB、14B Q4 ≈ 8GB、27B Q4 ≈ 16GB。
- 显存档位速查表：8GB 跑 4B 级、12GB 跑 7-8B、16GB 是甜点档、24GB 跑 32B Q4、32GB 让 27-32B 升 Q8。
- Ollama 一条命令上手：`ollama pull`（tag 带 `q4_K_M`）→ `ollama run` → `ollama ps` 看 `/GPU`。
- llama.cpp 用 `-ngl` 控制 GPU 层数，显存不足 offload 到 CPU，慢但能跑。
- OOM 排障三板斧：降上下文 → 降量化档 → offload/换更小模型。

下一章，把这几章的经验收成一张可以照抄的「选卡速查表」，再带你看一眼 vLLM、多卡、KV Cache 优化、MoE 这些进阶名词各自解决什么问题——以后听到不慌，也知道往哪个方向继续学。

---

## 第 5 章：选卡速查表与进阶方向

前面四章，你从「显存是什么」一路走到「用 Ollama 跑起本地模型」。最后一章做两件事：把前面所有经验收成一张**能直接照抄的选卡/评估速查表**；再带你看一眼 vLLM、多卡、KV Cache 优化、MoE 这四个进阶名词**各自解决什么问题**——不要求你现在掌握，只求以后听到不慌，知道往哪个方向继续学。

### 5.1 本地部署选卡/评估速查表

#### 5.1.1 按预算/用途的选卡决策表

选卡只有一个核心原则：**先看显存，再看型号**。显存决定装不装得下，其他参数决定快不快。按你的用途和预算，直接对照这张表：

| 你的情况 | 建议显存 | 参考 | 能跑到的模型上限 |
|----------|----------|------|------------------|
| 尝鲜/低预算 | 12GB 起步 | 入门级卡 | 7-8B 模型 Q4/Q8 |
| 日常学习主力 | 16GB | 中端卡 | 14B 模型 Q4（甜点档） |
| 认真跑大模型 | 24GB | RTX 4090 / 5090 级别 | 27B-32B 模型 Q4 |
| 追求更好质量 | 32GB | 高端卡 | 27B-32B 模型 Q8 完整装入 |
| 生产/服务化 | 80GB 以上 | A100/H100 级别 | 大参数模型 + 高并发 |

几个容易踩的边界，提前说清楚：

- **16GB 是多数人的甜点档**：能舒服跑 14B 级模型，性价比最高。
- **24GB（RTX 4090）**：个人本地能跑 27B-32B 的 Q4，是「个人认真玩」的主流选择。
- **70B 级模型**：即便 32GB 也装不下（Q4 也要 40GB 以上），必须靠卸载或生产级硬件，别指望消费级单卡。

#### 5.1.2 口诀：显存够，先升量化档，而不是换更大模型

整个本地部署圈最实用的一句口诀：

> **显存够，先升量化档，而不是换更大模型。**

升级显卡的成本远高于换个量化档位。16GB 卡想把体验往上提，优先把 14B 从 Q4 升到 Q8（质量明显变好，显存还够）；而不是硬上 32B 的 Q3——体积小了但质量下降明显，体验未必更好。显存不够时方向相反：先降档、再换小模型（第 4 章的 OOM 三板斧）。

#### 5.1.3 入门 / 进阶 / 生产三档配置建议

把上面的表浓缩成三档可直接照抄的「标准配置」：

| 档位 | 显存 + 模型 | 工具链 | 适合谁 |
|------|-------------|--------|--------|
| 入门 | 12GB 起步 + 7-8B Q4 | Ollama | 想体验本地模型、低成本跑通全流程 |
| 进阶 | 24GB（4090 级别）+ 27B-32B Q4 | Ollama / llama.cpp | 个人日常主力，兼顾质量与速度 |
| 生产 | 80GB 级 + FP8/BF16 | vLLM | 要服务多个用户、追求高并发与稳定 |

入门和进阶档，Ollama 一条命令就能跑；生产档通常用更高精度的 FP8/BF16 配合 vLLM，牺牲一点显存利用率换质量和吞吐。

### 5.2 进阶方向速览

下面四个名词，都是「显存不够用 / 想塞更大模型」催生出来的解法。逐个认识一下。

#### 5.2.1 vLLM 与 PagedAttention（分页复用 KV Cache）

**解决什么问题**：并发很多请求时，KV Cache 碎片化浪费。

传统推理时，每个对话会独占一块连续的显存存 KV Cache，聊完释放，中间留下很多没法利用的小碎片；请求一多，显存被碎片白白吃掉。**vLLM** 的高吞吐秘密在 **PagedAttention**：把 KV Cache 切成小块「页」，像操作系统管理内存一样按需分配、用完回收、整页复用。实测同环境比 Transformers 快约 2 倍、显存吃得更满。

> [!tip] 大白话
> 把 KV Cache 想成仓库货架。传统方式是每轮对话独占一整柜子，聊完柜子还锁着、别人不能用；vLLM 是共享货架，谁需要就取哪一格，格子随时回收复用。同样的仓库面积，能放下的对话多得多。

一个 vLLM 启动示例（参数来自实战案例：显存利用率 0.96、上下文 1024）：

```bash
# vLLM 启动示例（模型名按你实际拉的替换）
VLLM_USE_V1=0 vllm serve 模型名 --gpu-memory-utilization 0.96 --max-model-len 1024
```

> [!note] 提示
> 消费级单卡也能跑 vLLM（比如 24GB 卡用 NF4 4bit + 双量化硬上 32B），但 vLLM 的主要舞台是高并发服务。个人单机对话，Ollama/llama.cpp 通常更省心。

#### 5.2.2 多卡：张量并行（TP）

**解决什么问题**：一张卡装不下超大的模型。

**张量并行（Tensor Parallelism，TP）** 把权重按卡数分片，每张卡只放 1/TP 的权重，多张卡协作算同一个模型。TP=2 就是两张卡各放一半权重。它需要框架专门支持（vLLM、llama.cpp 等都支持多卡），成本高、配置复杂，个人很少用——主要用于模型大到单卡实在装不下的场景。

> [!tip] 大白话
> 把张量并行想成几个人合伙抬一张超大的桌子。一个人抬不动，就切成几块、每人抬一块，步伐一致地往前走。每人的负担只有原来的 1/TP，但所有人必须配合好，少了谁都不行。

#### 5.2.3 KV Cache 量化（NVFP4）与 GQA

**解决什么问题**：KV Cache 太大，吃掉本可留给权重的显存（第 2 章的「显存杀手」）。

两条路线，一条是「事后压缩」，一条是「天生省」：

- **NVFP4（KV Cache 量化）**：NVIDIA 把 KV Cache 从 16-bit/FP8 量化到 4-bit，显存比 FP8 再降约 50%，同样显存能容纳的上下文容量翻倍，精度损失小于 1%。目前主要面向 Blackwell 数据中心卡。
- **GQA（分组查询注意力）**：模型架构层面的优化，多个查询头共享一组 Key/Value，从源头减少 KV Cache 体积。选模型时，名字里带 GQA 的架构天生更省 KV Cache。

#### 5.2.4 MoE：下载小、显存不小的「陷阱」

**解决什么问题**：同样是「聪明的大模型」，MoE 架构下载小、算得快，但显存需求并不小。

**MoE（混合专家）** 模型总参数很大，但每次计算只激活一部分「专家」。问题在于：**推理时必须把全部权重都加载进显存**，一个都不能少。所以它有个反直觉的坑——下载体积小，不代表显存需求小。举个例子：一个 16B 的 MoE 模型，下载才 6GB，实际要占约 23GB 显存。判断装不装得下，永远看**总参数量 × 精度**，别被下载体积骗了。

> [!tip] 大白话
> 把普通模型（Dense）想成一栋普通房子，MoE 想成一栋大别墅。别墅整栋都要装修到位（显存必须装下全部权重），但住的时候每次只开一个客厅（只激活部分专家），所以住着不费电（算得快）。装修费按整栋算、电费按开灯的房间算——这就是「下载小、显存不小」的原因。

真实例证：DeepSeek-R1 有 671B 参数，加载时要装下全部 671B 权重，但激活计算只按约 37B 算——大参数的「月租」一分不少，省的是「按量计费」那部分。

### 5.3 学习路径延伸：下一步去哪

走完这条路线，你已经具备：

- 分得清显存、内存、硬盘，看懂显卡四大参数；
- 用公式 + 速查表估算「我的显卡能跑多大模型」；
- 用 Ollama 一行命令在本地跑起量化模型，并确认它在用 GPU；
- 遇到 `CUDA out of memory`，按三板斧自己排障。

下一步按兴趣选一条：

- **想深入原理**：精读一两篇官方显存估算文档（如阿里云 PAI），自己动手用公式核算不同模型的显存，把速查表背后的逻辑吃透。
- **想多玩模型**：去 Ollama 模型库、Hugging Face 试不同参数和量化档位的模型，亲手验证第 4 章速查表里的数据。
- **想上强度**：学 vLLM 部署、多卡张量并行、KV Cache 优化，把本地推理从「小玩具」做成「小服务」。
- **想追前沿**：关注 MoE、GQA、NVFP4 这些趋势，理解它们如何在不加显存的前提下塞进更大的模型。

---

显存是 GPU 的专属工作台——装不装得下看容量，快不快看带宽；大模型的显存账单，是权重的固定月租加上 KV Cache 的按量计费；量化是把大模型塞进显存的钥匙；而这张速查表，就是你以后每次动手前的对照清单。恭喜你走完这条路线，现在可以打开终端，跑出属于自己的第一个本地模型了。
