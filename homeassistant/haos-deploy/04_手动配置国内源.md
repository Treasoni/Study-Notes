---
title: "第四章：手动配置国内源（官方原版加速核心）"
tags:
  - 智能家居/HomeAssistant
  - HAOS/部署教程
  - 学习笔记
created: 2026-08-06
updated: 2026-08-07
status: 已完成
source_project: haos-deploy-tutorial
chapter: 4
---

# 第四章：手动配置国内源（官方原版加速核心）

> [!summary] 本章讲什么
> 本章解决官方原版 [[01_绪论|HAOS]] 在国内网络下的五大拉取瓶颈，按「先解决主线、再补其他」排序：**Supervisor 的 ghcr.io 镜像（4.1，官方 HAOS 就改这里）**、Docker 镜像（4.2，12 源故障转移链，仅普通 Linux / Supervised）、Add-on 商店（4.3，国内仓库 + 镜像映射）、NTP 时间（4.4，国内时间源 + 首次启动预置）、首次启动 DNS/ghcr 加速（4.5，udev 方案及失效提醒）。最后 4.6 给出没有宿主 SSH 时的替代路线。每一节都给出可直接复制的配置片段与验证命令。

## 0 先看这里：你的安装方式决定看哪节

| 你的环境 | 要改的核心文件 | 直达节 |
|---------|--------------|--------|
| 官方 HAOS（本教程主角） | `/usr/share/hassio/docker.json` | 4.1 |
| 普通 Linux + Docker | `/etc/docker/daemon.json` | 4.2 |
| Supervised 安装 | `/etc/hassio.json` + `/usr/share/hassio/docker.json` | 4.1 |
| 任何方式都要做 | `/etc/systemd/timesyncd.conf` | 4.4 |
| 连宿主 shell 都进不去 | 直接改 docker.json 或先解锁宿主 | 4.6 |

> [!tip] 一秒判断你在不在宿主 shell
> 提示符形如 `[ad19d24a-ssh hassio]$`、或敲 `systemctl` 报 `command not found` → 你在**加载项容器**里，先看 4.6（有 docker 权限可直接改，不用进宿主）。

> [!warning] 时效性说明
> 所有镜像站地址、仓库地址均为采集时间（2026-08-06）可用的社区源，公益镜像站隔天失效是常态，以实际拉取结果为准；失效回到 4.1 / 4.2 换源即可。udev 覆盖方案已随 2026.2.1 失效，详见 4.5。

> [!note] 先读：改系统文件要进「宿主 shell」，不是加载项
> 本章要改的都是**系统级配置文件**，`ha` 命令（如 `ha core restart`）只能控制服务，不能编辑 `/etc/` 下的文件。真正能改文件的是**宿主 shell**，只有两个入口：
> - **VM 控制台 `login`**（零配置、随时可用）：控制台输入 `login` 进 root shell；
> - **宿主 SSH 22222**：`ssh root@<IP> -p 22222`，需先开启（见下）。
>
> ⚠️ **加载项 Terminal & SSH 不是宿主 shell**——它是**容器**（提示符 `[xxx-ssh hassio]$`、`systemctl` 不可用），只能跑 `ha` 命令，改不了宿主文件。
>
> **宿主 SSH（22222）怎么开？**（开发者功能，无 Web 开关）
> - **① CONFIG 分区放公钥（官方，推荐）**：卷标 `CONFIG`（全大写）的分区/U 盘根目录放 `authorized_keys`（无扩展名、LF 换行、ASCII），插上重启（或 `ha os import`），无需先完成引导；
> - **② VM 控制台 `login`**：零配置，适合一次性改文件。
>
> > [!warning] 鸡生蛋陷阱
> > 加载项要从商店安装，而商店拉镜像正是本章要解决的 ghcr 瓶颈。**不要**指望先装加载项再改源；改系统文件优先用控制台 `login` 或 22222。
>
> > [!tip] 一句话流程（VMware 最省事）
> > 烧录 → 开机 → **VM 控制台 `login`** 直接改文件；想远程就按 ① 的 CONFIG 公钥法。首次引导卡"拉 700MB 组件"就先按 4.4 预置时间源。
>
> **适用性**：官方 HAOS 根文件系统**只读**，`/etc/docker/daemon.json`（4.2）改不了；真正要改的是 **4.1 的 `/usr/share/hassio/docker.json`** 和 **4.4 的 `/etc/systemd/timesyncd.conf`**。

## 4.1 Supervisor 镜像源：破解 ghcr.io 拉取瓶颈（官方 HAOS 主线）

这是官方原版解决 ghcr.io 的核心路径，也是本章最值钱的一节。先讲原理：HA 架构里 Supervisor 是"调度中枢"，Core 和所有 Add-on 的容器镜像都由它负责拉取，而这些镜像全部托管在 GitHub Container Registry（ghcr.io）。国内直连 ghcr.io 经常超时，表现就是 Add-on 装不上、Core 更新拉不动、系统卡在启动的转圈界面。

配置分两个文件，各管一件事（**官方 HAOS 只改文件二**）：

**文件一：`/etc/hassio.json`** —— 改 Supervisor 自身的镜像地址，让 Supervisor 拉取/更新自身时走国内镜像：

> [!warning] 官方 HAOS 没有这个文件，跳过文件一
> `/etc/hassio.json` 只在 **Supervised 安装**（普通 Linux + Docker，由安装脚本生成）下存在且可改。官方 HAOS 的 `/etc` 位于只读根文件系统，**该文件不存在也不可编辑**。官方 HAOS 上只需改下面的 **文件二** `/usr/share/hassio/docker.json`。

```bash
# /etc/hassio.json：改 supervisor 自身镜像（仅 Supervised 安装）
#   "supervisor": "ghcr.nju.edu.cn/xjboss/{arch}-hassio-supervisor"
#   {arch} = amd64（x86_64 实体机/虚拟机）或 aarch64（ARM 派）
```

**文件二：`/usr/share/hassio/docker.json`** —— 加 `registries_mirror`，把 ghcr.io 和 docker.io 的拉取整体映射到国内镜像：

> [!tip] 文件不存在？直接 `vi` 新建即可
> `/usr/share/hassio/docker.json` 默认可能不存在，`vi` 打开保存时自动创建。官方 HAOS 的 `/usr/share/hassio/` 指向可写的持久分区（对应 `/mnt/data/supervisor`），不是只读根，放心新建。

```bash
vi /usr/share/hassio/docker.json
```

```json
{
  "registries": {},
  "registries_mirror": { "ghcr.io": "ghcr.nju.edu.cn", "docker.io": "docker.nju.edu.cn" }
}
```

保存后重启 Supervisor 使配置生效：

```bash
systemctl restart hassio-supervisor
```

重启后去 Add-on 商店安装一个之前装不上的加载项，速度应明显改善。也可以观察 Supervisor 日志确认它走的是国内源，而不是卡在 ghcr 超时。

> [!warning] 易错点：升级会重置，必须先备份
> HAOS 系统升级时 `/usr/share/hassio/docker.json` 很可能被重置为默认值，导致加速悄悄失效。建议把这个文件复制一份备份，升级后对比恢复：

```bash
cp /usr/share/hassio/docker.json /usr/share/hassio/docker.json.bak
```

> [!note] 能力边界
> `registries_mirror` 只解决**镜像拉取**，不解决 GitHub 源码下载和 OTA 升级。Add-on 仓库从 GitHub 拉源码、系统更新走官方 OTA，这些在国内仍可能慢，需要配合 4.3 的国内仓库，以及第 5 章讲到的国内 OTA 方案。

### 4.1 补充：系统级 OTA 能不能自建国内源？

> [!note] 常见思路：把官方 OTA 包搬到 Gitee，让 HA 从我的仓库更新
> 思路方向正确（国内 OTA 镜像正是第 5 章 [[05_HAOS-CN极速版]] 的核心卖点），但**官方原版上做不通**，四个障碍：
> 1. **没有配置入口**：OS 更新下载地址在 Supervisor 内部写死，指向 GitHub 官方 Releases，不是 4.1 的 `docker.json` 能改的；
> 2. **根文件系统只读**：squashfs/EROFS 只读，改不了内置端点——这正是 HAOS-CN 必须在构建时硬编码重定向的原因；
> 3. **要重定向两个端点**：版本检查（`version.home-assistant.io`）和下载端点都要改，只搬 .raucb 到 Gitee，HA 根本不知道去哪找；
> 4. **Gitee 放不下**：官方 .raucb 约 500MB–1GB，Gitee 免费仓库单文件一般限 100MB，LFS 免费配额也扛不住持续更新（以 Gitee 当前规则为准）。
>
> 好的一面：RAUC 更新包有官方签名，搬运官方包签名校验能过——卡住你的不是签名，是上面 1–4。

> [!tip] 可行替代：本地 `rauc install`（不用 Gitee，不改系统）
> 想要的效果「不依赖官方 OTA 的国内慢速」，本地安装即可达成——在任意有网的机器上下载官方 OTA 包，传到 HAOS 持久分区再本地安装：
>
> ```bash
> # 1) 在任意有网的机器上，从 GitHub 拉官方 OTA 包（gh-proxy 加速）
> wget -c https://gh-proxy.org/https://github.com/home-assistant/operating-system/releases/download/12.5/haos_generic-x86-64-12.5.raucb
> # 2) 传到 HAOS 持久分区（/mnt/data 可写）
> scp haos_generic-x86-64-12.5.raucb root@<IP>:/mnt/data/
> # 3) SSH 登录，本地安装（保留 RAUC 签名校验 + A/B 槽位 + 失败自动回滚）
> ssh root@<IP> -p 22222
> rauc info  /mnt/data/haos_generic-x86-64-12.5.raucb
> rauc install /mnt/data/haos_generic-x86-64-12.5.raucb
> reboot
> ```
>
> - 板型名：VMware/PVE/实体机 x86-64 是 `generic-x86-64`，树莓派是 `rpi4-64` 等；
> - `rauc` 基于 HAOS 的 A/B 更新框架，root shell 一般可用；装前先按第 7 章做一次备份；
> - 版本号 `12.5` 仅为示例，以官方 Releases 实际版本为准。

> [!tip] 全自动国内 OTA 的现成方案
> 想让「HA 自动从国内 OTA 更新」，直接切第 5 章 [[05_HAOS-CN极速版]]：版本检查 + OTA 下载都重定向到国内，保留 RAUC 签名 + A/B 原子更新，官方版一键转过去：
>
> ```bash
> login
> curl -fsSL https://ota.hasscn.top/upgrade.sh | bash
> ```
>
> 代价是第 5 章的信任模型（非官方、校验和缺口、硬依赖中国 IP）。

| 方案 | 效果 | 代价 |
|------|------|------|
| 官方 OTA（默认） | 国内慢/易失败 | 零改动 |
| 本地 `rauc install` | 升级速度自己掌控，官方签名保真 | 每次手动下载 + scp + 安装 |
| [[05_HAOS-CN极速版\|HAOS-CN]] | 全自动国内 OTA | 信任模型 + 无校验和 |

## 4.2 Docker 镜像加速：12 源故障转移链（仅普通 Linux + Docker / Supervised）

国内直连 Docker Hub（docker.io）拉镜像普遍慢或超时，根因是官方 Registry 节点没有覆盖大陆网络。Docker daemon 提供 `registry-mirrors` 配置：拉取 docker.io 镜像时自动改走镜像站，镜像站会回源缓存，命中缓存的镜像几乎秒下。

> [!warning] 本节不适用于官方 HAOS
> 官方 HAOS 根文件系统**只读**，`/etc/docker/daemon.json` 改了也无效；官方 HAOS 走 **4.1** 改 `/usr/share/hassio/docker.json`。本节只适用于**普通 Linux + Docker 或 Supervised 安装**。

这段 12 源配置本质是一条"故障转移链"。daemon 会按数组顺序依次尝试，前面某个源连接超时或返回 429 时自动落到下一个，直到成功。这就是为什么公益源不稳定也不用怕——多配几个，坏一两个不影响整体。

> [!warning] 动手前先确认：文件不存在是正常的
> `/etc/docker/daemon.json` 不是 Docker 默认自带文件，**不存在是正常的**，需要先手动创建：`mkdir -p /etc/docker` 再新建该文件。

配置写入 `/etc/docker/daemon.json`（完整 12 源示例）：

```json
{
  "registry-mirrors": [
    "https://docker.1panel.live", "https://docker.1ms.run", "https://dytt.online",
    "https://docker-0.unsee.tech", "https://lispy.org", "https://docker.xiaogenban1993.com",
    "https://666860.xyz", "https://hub.rat.dev", "https://docker.m.daocloud.io",
    "https://demo.52013120.xyz", "https://proxy.vvvv.ee", "https://registry.cyou"
  ]
}
```

改完后重启 Docker 并验证镜像源是否生效：

```bash
systemctl restart docker
docker info | grep -A5 "Registry Mirrors"
```

看到列出的 12 个地址即生效。再拉一个小镜像实测速度：

```bash
docker pull hello-world
```

能秒下说明至少有一个源是通的。除了这 12 个公益源，还有 `docker.nju.edu.cn`、`docker.mirrors.ustc.edu.cn`，以及需申请专属地址的阿里云加速器（`<你的ID>.mirror.aliyuncs.com`）。源越多，故障转移的兜底越厚。

两个关键坑必须记住：

> [!warning] 坑一：`registry-mirrors` 只对 docker.io 生效
> 它只改写 docker.io 的拉取地址，对 **ghcr.io 无效**。而 HA 的核心镜像和官方 Add-on 镜像恰恰托管在 ghcr.io——这部分需要走 4.1 的 Supervisor 层配置，两条路径互补，缺一不可。

> [!warning] 坑二：原版 HAOS 根文件系统只读
> 在 HAOS 里 `/etc/docker/daemon.json` 位于只读根文件系统，**无法直接编辑**。这条配置真正落地的场景是：你在跑普通 Linux + Docker（或 Supervised 安装）；HAOS 场景请回到 **4.1**，或直接用内置了 12 源故障转移的 [[05_HAOS-CN极速版|HAOS-CN]]（见第 5 章）。

> [!tip] 实操建议
> 公益镜像站隔天失效很常见，所以"多配几个 + 故障转移"比"只信某一个"更重要。如果哪天发现镜像拉不动，先 `docker pull` 单测判断是不是源挂了，再决定是否替换清单里的失效项。配好之后把拉取命令存成脚本，每月巡检一次（第 8 章有周期性清单）。

## 4.3 Add-on 商店国内仓库与镜像映射

Add-on 商店默认的仓库列表大多指向 github.com / ghcr.io，国内加载经常转圈或直接失败。解决思路是双管齐下：添加一个托管在 Gitee 的国内仓库作为安装源，再用镜像映射表把拉取地址替换成国内可达地址。

添加国内仓库的入口在 Web 界面：

> [!example] 添加仓库
> 设置 → 加载项商店 → 右上角三个点 → 仓库 → 粘贴以下地址并添加：
>
> ```text
> https://gitee.com/desmond_GT/hassio-addons
> ```

添加后，商店里出现的加载项安装时，镜像地址按下面这张映射表替换（正文平铺，可直接对照）：

| 原地址                 | 国内镜像                 | 用途                          |
| ------------------- | -------------------- | --------------------------- |
| ghcr.io             | `ghcr.nju.edu.cn`    | Add-on 容器镜像（核心加速项）          |
| docker.io / lscr.io | `docker.1panel.live` | Docker Hub 与 LinuxServer 镜像 |
| github.com          | `gh-proxy.org`       | GitHub 仓库源码 / 更新            |

理解一下机制：每个 Add-on 的 manifest 里声明了 `image` 字段，Supervisor 拉取时按这张映射把镜像地址改写成国内镜像。所以映射表和 4.1 的 `registries_mirror` 是一套思路的两种实现，前者管 Add-on 声明，后者管全局镜像名。

> [!warning] 易错点
> 配置后**无需翻墙**，翻墙反而可能因为代理链路导致失败；国内镜像首次安装偶发失败是正常的，**重试一次即可**。另外注意 GitHub 仓库仅用于更新源列表，实际安装走 Gitee 地址，两者不冲突。如果添加仓库后列表不刷新，先在系统日志确认是网络问题还是地址过期。

## 4.4 NTP 时间同步国内化（含首次启动预置）

时间不同步是国内 HAOS 一个隐蔽的"稳定性杀手"：HTTPS 证书校验、自动化定时触发、更新检查、日志时间戳全部依赖准确时间。HAOS 默认指向国际 NTP 源，国内网络经常超时，导致系统时间持续偏移，轻则日志时间错乱，重则证书校验失败、更新拉不下来。

通过 SSH（root，端口 22222）登录后，编辑时间同步配置：

```ini
# /etc/systemd/timesyncd.conf
[Time]
NTP=ntp1.aliyun.com ntp2.aliyun.com ntp3.aliyun.com
FallbackNTP=ntp.tencent.com cn.pool.ntp.org
```

重启时间同步服务并验证：

```bash
systemctl restart systemd-timesyncd.service
timedatectl
```

看到 `System clock synchronized: yes` 即同步成功，此时时间已经对准国内源。除了阿里云（`ntp1~7.aliyun.com`）、腾讯（`ntp.tencent.com`），还可用 `ntp.ntsc.ac.cn`（中科院国家授时中心）作为备选。多填几个，一个挂了自动切换。

> [!tip] 首次启动前预置（强烈建议）
> 系统还没跑起来之前，在烧录后的 `hassio-boot` 分区的 `CONFIG/timesyncd.conf` 里放好同样的配置（**权限设为 644**），首次启动即可在 10 秒内完成时间同步。这一点对首次启动至关重要：首次启动要联网拉取约 700MB 的 Core 组件，如果时间没对齐，HTTPS 证书校验会失败，导致拉取莫名其妙失败或极慢。

容器环境（Supervised / 普通 Linux 跑 HA）没有 `timesyncd` 时，改用 **chrony 加载项**：把 `ntp_pool` 改成 `cn.pool.ntp.org`，并开启开机自启。

> [!warning] 时间异常判别法
> - 差**整 8 小时**：是时区未设（没设 Asia/Shanghai），不是 NTP 问题，去设置里改时区。
> - 差**几分钟且持续增大**：是 NTP 同步失败，回上面对比 `timedatectl` 的同步状态。
> 分不清时区问题与同步问题时，先 `timedatectl` 看 `Time zone` 一行再决定处理方向。确认时区用 `timedatectl set-timezone Asia/Shanghai`。

## 4.5 首次启动 DNS/ghcr 加速补充与方案失效提醒

由于原版 HAOS 根文件系统只读，社区曾用一个技巧绕开：用 **udev bind-mount** 在 Docker 服务启动前，把宿主机上的 `daemon.json`（含 `proxies` 配置走 ghcr 代理）覆盖挂载进系统，从而在首次启动拉 ghcr 镜像时实现加速。原理是在只读根上做一层"可写覆盖"，相当于把一个外部文件绑定到只读路径上。

> [!warning] 方案已失效（2026.2.1）
> 该 udev 覆盖方案在 **2026.2.1 版本起失效**。新版 HAOS 原样照抄旧教程的整套 udev 规则会 **boot loop**，需要精简字段才可能适配。采集时间（2026-08-06）的最新版本上，这个方案已不建议作为主线，只作原理理解。看到 2025 年及更早的教程让你"直接复制整套 udev 规则"，务必保持警惕。

> [!tip] ghcr 拉取困难的现实推荐排序
> 按"省心程度 + 有效性"排序，解决 ghcr 拉取瓶颈的优先顺序：
> 1. 国内定制版（[[05_HAOS-CN极速版|HAOS-CN]]，第 5 章）——内置全套加速，最省心；
> 2. Supervisor `registries_mirror`（4.1）——官方原版的正路；
> 3. udev bind-mount——已被版本淘汰，仅当跑旧版时考虑；
> 4. 手拉镜像改名——手动 pull 国内源再 tag 成 ghcr.io 地址，最费事但永远可用作应急。
> 若连宿主 shell 都进不去，先用 4.6 解锁宿主访问，再回到这里。

> [!tip] 首次启动实测预期
> 首次启动联网拉取约 700MB 组件，国内网络下慢是正常的（数小时也可能出现），**期间不要断电**。加速的正确姿势是：先按 4.4 预置 `timesyncd.conf` 保证时间对齐，再靠 4.1 的 Supervisor 镜像源加速 ghcr 拉取；等不了的话，直接换第 5 章的 `-full` 完整包免初始化等待。给这台机器配个静态 IP 也能避免重启后 DHCP 重试导致的等待（第 8 章有排查）。

## 4.6 没有宿主 SSH 时的替代路线（直接改文件或打开宿主 shell）

前面 4.1–4.5 都要改**系统级文件**，通常前提是能进宿主 shell。如果你连 22222 都进不去（没配公钥、又不想动控制台），先看这节——**有 docker 权限时，4.1 的 `docker.json` 可以不用宿主 shell 直接改**（方案 A）。

> [!warning] 容器识别速查：你不在宿主 shell
> 「Terminal & SSH」加载项跑在**容器**里，不是宿主系统，在里面改系统文件、跑 `systemctl` 都会失败：
> - 提示符形如 `[ad19d24a-ssh hassio]$`（`xxx-ssh hassio`）→ **容器**
> - 执行 `systemctl` 报 `command not found` → **容器**
> - 宿主 shell 里 `systemctl` 一定可用，提示符通常为 `root@homeassistant:~#`

**方案 A（有 docker 权限时最快）：直接用 docker 改 `docker.json`，不进宿主 shell**

适用：加载项有 docker 权限、只想配 ghcr 镜像加速、不想进宿主 shell。原理：`hassio_supervisor` 容器把宿主的 `/usr/share/hassio` 挂载为自己的 `/data`，所以**容器里的 `/data/docker.json` 就是宿主的 `/usr/share/hassio/docker.json`**。

```bash
# 1) 确认能看到 supervisor 容器，且 /data 对应宿主 /usr/share/hassio
docker ps | grep hassio_supervisor
docker exec hassio_supervisor ls -la /data/

# 2) 写入 /data/docker.json（= 宿主的 /usr/share/hassio/docker.json）
cat <<'EOF' | docker exec -i hassio_supervisor sh -c 'cat > /data/docker.json'
{
  "registries": {},
  "registries_mirror": { "ghcr.io": "ghcr.nju.edu.cn", "docker.io": "docker.nju.edu.cn" }
}
EOF

# 3) 验证 + 重启 Supervisor 生效（等价宿主 systemctl restart hassio-supervisor）
docker exec hassio_supervisor cat /data/docker.json
ha supervisor restart

# 4) 备份（HAOS 升级可能重置该文件）
docker exec hassio_supervisor sh -c 'cp /data/docker.json /data/docker.json.bak'
```

> [!warning] 注意
> JSON 必须合法，否则 Supervisor 可能起不来 Docker，写完先 `cat` 确认。这只解决 4.1 的镜像加速；要改 4.4 的 NTP 等系统文件，仍需进宿主 shell（见方案 B/C/D）。

下面的方案 B–E 是「进宿主 shell」的替代路线，按省事程度排序：

**方案 B（推荐，零配置）：VM 控制台 `login`**

适用：最快进宿主 shell 的方式，适合一次性把 4.1 / 4.4 的系统文件改完。核心理解：**VM 控制台 = 这台虚拟机的「屏幕窗口」**——就像给机器接了一台显示器，在窗口里操作与网络、SSH、浏览器都无关，是机器本体的调试入口。它和加载项终端（容器）是完全不同的两个东西。

VMware Workstation / Player 步骤：

1. 左侧库列表找到 HAOS 虚拟机，**双击打开它的窗口**（即显示虚拟机屏幕的大窗口；VMware 主界面本身不是控制台）。
2. 等启动日志滚完，停在 `Home Assistant OS login:`（或 `homeassistant login:`）提示符。
3. 输入 `login` 回车——这是 **HAOS 的开发者调试命令，不是用户名**，会免密码直接进 root shell，提示符变为 `root@homeassistant:~#`。
4. 验证进对了：`systemctl --version` 能输出版本号即宿主系统（加载项容器里会 `command not found`）；`hostname` 应显示 `homeassistant`。
5. 然后正常执行 4.1 / 4.4 的命令（如 `vi /usr/share/hassio/docker.json`、`systemctl restart hassio-supervisor`）。

> [!tip] 操作技巧
> 在 VM 窗口内点击后，鼠标键盘会被虚拟机捕获；按 **Ctrl+Alt** 释放鼠标回 Windows。PVE 用户在虚拟机列表点该机 → 右上角「Console」（noVNC 网页控制台），同样输入 `login`。

**方案 C：CONFIG U 盘解锁宿主 SSH 22222（官方方法，可远程）**

适用：不依赖加载项、想长期从 Windows 远程管理宿主。准备一个 U 盘/分区，卷标命名 `CONFIG`（全大写），根目录放 `authorized_keys`（无扩展名、LF 换行、ASCII，内容为 SSH 公钥），插上重启自动导入（或 `ha os import`）。之后 `ssh root@<IP> -p 22222`。VM 场景就是把虚拟 U 盘挂给虚拟机。

**方案 D：从加载项一行命令解锁宿主 SSH 22222（社区方法，需 Docker 权限）**

适用：已有 docker 权限、想顺手把宿主 SSH 长期打开。原理：利用加载项容器的 Docker 权限，把自己的公钥写进 HAOS 的 CONFIG 分区。前置检查：

```bash
which docker        # 有输出 = 加载项有 Docker 权限；没有则先开加载项配置里的 Docker 访问
lsblk               # 确认引导分区：VMware 通常 /dev/sda1，NVMe /dev/nvme0n1p1，树莓派 /dev/mmcblk0p1
```

然后在加载项终端执行（把 `/dev/sda1` 换成你看到的引导分区）：

```bash
(BOOT_PART=/dev/sda1; cat /data/.ssh/authorized_keys | sudo docker exec -i homeassistant bash -c "mkdir -p /mnt/temp && mount $BOOT_PART /mnt/temp && mkdir -p /mnt/temp/CONFIG && cat >| /mnt/temp/CONFIG/authorized_keys && umount /mnt/temp && rmdir /mnt/temp")
```

> [!warning] 注意
> - 加载项若用密码登录（没有密钥文件），把 `cat /data/.ssh/authorized_keys` 换成 `echo '你的 SSH 公钥'`。
> - 执行完 `reboot`，重启后 `ssh root@<IP> -p 22222` 即可进宿主。
> - 这是社区方法，操作前用 `lsblk` 确认引导分区别选错；方法源自 HAOS 官方开发者文档的调试思路。

**方案 E：直接换 [[05_HAOS-CN极速版|HAOS-CN]]（第 5 章，连宿主都不用进）**

适用：不想碰任何系统文件、接受非官方信任模型。内置全套国内加速（ghcr 重定向 + 12 源故障转移），在加载项里一行切换：

```bash
login
curl -fsSL https://ota.hasscn.top/upgrade.sh | bash
```

代价是信任模型（非官方 + 校验和缺口 + 硬依赖中国 IP），详见第 5 章。

> [!tip] 一句话建议
> 只想配镜像加速：方案 A（docker 直接改）。一次性完整配置：方案 B（控制台 `login`）。长期远程管理：方案 C 或 D 解锁 22222。不想碰系统文件：方案 E 换 HAOS-CN。

## 本章 Checklist

- [ ] 会识别宿主 shell 与加载项容器（提示符 / `systemctl` 是否可用）
- [ ] 官方 HAOS 已在 `/usr/share/hassio/docker.json` 配置 `registries_mirror`（ghcr.io → ghcr.nju.edu.cn）
- [ ] 已 `systemctl restart hassio-supervisor` 并验证 Add-on 安装提速
- [ ] 已备份 `docker.json`，知道升级可能重置它
- [ ] 理解 `registry-mirrors`（4.2）只对 docker.io 生效，对 ghcr.io 无效
- [ ] 知道原版 HAOS 根文件系统只读，`/etc/docker/daemon.json` 不可直接改（4.2 仅普通 Linux / Supervised）
- [ ] 知道系统级 OTA 无法像容器镜像一样配国内源（URL 写死 + 根只读），需要时用本地 `rauc install` 或切第 5 章 HAOS-CN
- [ ] 已在 Add-on 商店添加 Gitee 国内仓库（`https://gitee.com/desmond_GT/hassio-addons`）
- [ ] 知道镜像映射表：ghcr.io / docker.io / lscr.io / github.com 各自走哪个国内镜像
- [ ] 已配置 `timesyncd.conf` 国内 NTP 源并 `timedatectl` 确认 `System clock synchronized: yes`
- [ ] 首次启动前已在 `hassio-boot/CONFIG/timesyncd.conf` 预置时间源（权限 644）
- [ ] 能区分"差整 8 小时（时区）"与"持续增大（NTP）"
- [ ] 知道 udev bind-mount 方案已随 2026.2.1 失效，不在新版本原样照抄
- [ ] 理解 ghcr 加速的推荐排序：HAOS-CN > Supervisor registries_mirror > udev > 手拉镜像
- [ ] 进不去宿主 shell 时，知道 4.6 的替代路线（docker 直接改 / 控制台 login / CONFIG U 盘 / 加载项解锁 / HAOS-CN）

---

> 下一章预告：官方原版的手动加速到这里已全部配齐。如果你不想逐层折腾，下一章介绍 [[05_HAOS-CN极速版|HAOS-CN]] 极速版——内置全套国内加速的一键替代方案，并如实列出它的信任模型与风险边界。

---

> ⬅️ 上一章：[[03_官方原版HAOS安装实战|第三章：官方原版 HAOS 安装实战（实体机 + 虚拟机全平台）]] ｜ 📖 [[部署 HAOS 详细教程|返回索引]] ｜ 下一章：[[05_HAOS-CN极速版|第五章：HAOS-CN 极速版——一键国内化替代方案]] ➡️
